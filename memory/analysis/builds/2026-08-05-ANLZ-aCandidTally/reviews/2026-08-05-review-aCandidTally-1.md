# review-aCandidTally-1 - Tier-2 adversarial spec review

**Run:** wf_572b24b3-f3c - 2026-08-05 - node a - 15 agents - 5 finder lenses, 3 batched
skeptics, 1 synthesis. Target: ANLZ-aCandidTally-1 rev-2 at base 6920f017.

Precision 28/(28+16) = 0.64. Every finding below was checked against source by a skeptic
prompted to refute it. The synthesized AMENDMENTS block is folded into the spec at rev-3.

## Confirmed

| # | Severity | Lens | Spec section | Finding |
|---|---|---|---|---|
| C1 | blocker | claims | S6, §4 Files touched, AC8 | S6's canonicalVersion 2->3 bump breaks two existing Test-Analyze.ps1 assertions the spec never lists |
| C2 | major | claims | S6, §4 Files touched, §7 Gates | canonicalVersion 2->3 makes the kickoff manifest's byte-identity trap stale; the manifest is not in Files touched |
| C3 | major | claims | §4 Data model, §4 Rollout, AC5 | The defect characterization and AC5's byte-identity precondition ignore case-insensitive collision |
| C4 | major | claims | AC2, §4 Inventory, §5 i18n | Repaired empty-display-name metrics publish an EMPTY metric label into the headline and WIN/LOSS prose |
| C5 | minor | claims | AC8 | AC8's suite-count arithmetic is wrong: the other six suites total 366, not 1064 |
| C6 | blocker | completeness | section 4 Rollout + AC5 (and the Inventory reachability paragraph, lines 98-100) | Byte-identity precondition is false: display names differing only in case are a third, un-enumerated flip class |
| C7 | major | completeness | section 4 Inventory table (row "reconciliation rules") and S4 | Inventory omits the native-ratio reconciliation sites at :528 and :545 |
| C8 | major | completeness | S3 / Inventory row "headline presence" / AC2 / section 5 i18n | Metrics with an empty display name are un-dropped into a client-facing surface with a blank label |
| C9 | major | completeness | F4 resolution (spec:231-235), S5, section 5 i18n, Inventory row "breakdown labels" | F4's "no report-template change" is unsupported: the uniquified key becomes the client-facing breakdown label, and it is an internal id |
| C10 | major | completeness | S2, section 4 Data model, S4 | S2 defines no metric-object-to-key mapping, and :647 resolves a metric belonging to a different widget |
| C11 | major | completeness | S6, section 4 Files touched (Test-Analyze.ps1 row), AC8 | Test flip set omits two in-place canonicalVersion assertions; AC8's "additive only" is false as written |
| C12 | minor | completeness | section 4 Files touched, section 5 user docs, section 7 Gates | Doc flip set omits the two governing records that assert the U9 waiver is the SOLE canonicalVersion exception, and the manifest re-stamp is unbudgeted |
| C13 | blocker | newbreakage | S2 (spec:18-20), S7 (spec:31-33), AC4 (spec:188-189) | S2's replay map has no pinned comparer, and a case-sensitive one silently re-creates the exact defect this unit fixes |
| C14 | major | newbreakage | S2 (spec:18-20), S9 (spec:36-38) | S2 does not say WHERE the fallback runs, and the 135 -DefineOnly fixtures build metric records with no cellKey |
| C15 | major | newbreakage | S5 (spec:25-26), production-readiness i18n (spec:163-164), files-touched (spec:130-138) | S5 deletes the only human-readable metric label in the breakdown, and the i18n bullet asserting no user-visible string changes is false |
| C16 | minor | newbreakage | AC5 (spec:190-191), Rollout (spec:119-124) | The byte-identity precondition says "unique", but Uniq-Key's uniqueness test is case-insensitive, so a case-differing pair flips while satisfying AC5 as written |
| C17 | minor | newbreakage | AC8 (spec:196-198), S9 (spec:36-38) | AC8's assertion-count arithmetic is wrong: 1064 is the eight-suite total, not the other six |
| C18 | blocker | byteidentity | section 4 Rollout (spec:119-124) + AC5 (spec:190-191), also section 4 Data model (spec:60-62) | AC5/Rollout byte-identity precondition is false: names unique only case-sensitively still flip |
| C19 | major | byteidentity | AC5 (spec:190-191); section 4 Rollout (spec:119-124) | AC5 has no verification method and, as written, demands the full-facts golden this repo already rejected |
| C20 | major | byteidentity | S6 (spec:27-30) vs S3/S4/S5 (spec:21-26); inventory row spec:89 | The disclosure's "old value" is not derivable under S3-S5, and cannot exist at all for the empty-name case |
| C21 | major | byteidentity | S7 (spec:31-33) and AC4 (spec:188-189) | S7/AC4 parity gate is green-by-absence: no fixture matrix, and the replay's comparer is unpinned |
| C22 | major | byteidentity | S6 (spec:27-30), section 4 Files touched (spec:136), AC8 (spec:196-198) | The canonicalVersion 2->3 bump breaks two existing assertions the spec does not list, and AC8 says the suites are additive |
| C23 | blocker | contract | S6 / §4 Files touched / AC8 | S6's canonicalVersion bump reds two existing Test-Analyze assertions the spec never names |
| C24 | blocker | contract | S6 / F4 | S6 does not say where the flip disclosure lives, and the wrong placement makes the superseded (wrong) value closer-traceable |
| C25 | major | contract | AC6 | AC6's stated equality is arithmetically impossible; the closer's bag grows per breakdown cell, not per dropped metric |
| C26 | major | contract | §3 Non-goals (last bullet) | §3's non-goal for Analyze-SwydoTrend.ps1 rests on a false premise — it consumes the repaired headline |
| C27 | major | contract | S4 / AC2 | The repaired empty-display-name metric surfaces with an EMPTY label in both the report facts and the trend RECON findings |
| C28 | minor | contract | §4 Files touched (estimate) / S6 | The ratified 'sole exception' records for canonicalVersion are not superseded or re-stamped |

## Refuted

| # | Severity | Lens | Finding | Why it was dropped |
|---|---|---|---|---|
| R1 | blocker | claims | Inventory omits Get-RatioReconFindings (:528, :545) — the only omitted surface that emits a MAJOR finding | The factual half checks out: Analyze-SwydoReport.ps1:528 (`$unitOk = ($null -ne (Row-Cur $tr $rm.name))`) and :545 (`$numv=Row-Cur $tr $num.name; $denv=Row-Cur $tr $den.name; $repv=Row-Cur $tr $rm.name`) are display-name-keyed and are the only Row-Cur sites absent from the inventory table (I enum... |
| R2 | major | claims | S6 does not say where the flip disclosure lives, and it contradicts the U9 precedent it cites | The finding's load-bearing risk claim is false against the closer. Build-FactIndex (Test-ReportNumbers.ps1:212-219) enumerates the headline's METRIC KEYS and then reads exactly three named sub-fields per entry -- $h.displayCurrent, $h.displayPrevious, $h.displayDelta. It does not enumerate a head... |
| R3 | minor | claims | "Three readers gate on the literal schemaVersion 2" — only two exist in code; the third is prose | The finding's own evidence confirms the spec rather than contradicting it. A repo-wide grep (excluding memory/) returns exactly two code gates on the literal 2, and both throw: Analyze-SwydoReport.ps1:678 and ConvertTo-SwydoTrendFacts.ps1:75. The third gate is the model-side instruction at skill/... |
| R4 | minor | claims | S9's NW fixture cannot feed the analyzer's helpers directly — Normalize-Widget returns OrderedDictionary rows and Row-Label silently returns 1 | The PowerShell facts check out exactly: driving the real Normalize-Widget, a data row's dimensions is a System.Collections.Specialized.OrderedDictionary, @($row.dimensions.PSObject.Properties) has Count 7 with first property Name='Count' Value='1', so Row-Label returns '1'; after a ConvertTo-Json... |
| R5 | major | newbreakage | S4 names no carrier for the resolved key, and the cross-widget site at :646-647 resolves it against the wrong widget | The spec does name the carrier, one section earlier than the finding looks. S2 (spec:18-19) states the analyzer "reads `$m.cellKey` when present" — the key rides on the metric record, which is exactly what the finding's proposed fix asks for. Under that carrier every site the finding lists alread... |
| R6 | major | newbreakage | Row-Cur/Row-Cmp turn an unrepointed or unresolved key into a silent metric drop, and S4 explicitly declines to guard them | The PS 5.1 mechanics check out — I probed it: `$psCustomObject.$null` and `$psCustomObject.''` both return $null with no error, and Row-Cur (Analyze-SwydoReport.ps1:192) returns $null for both. But the failure scenario built on top of them is wrong. A call site S4 forgets to repoint does not pass... |
| R7 | major | newbreakage | S6's in-facts disclosure adds keys to every facts document, contradicting AC5's byte-identity claim | The contradiction is manufactured from a choice the finding itself admits the spec never makes ("The spec never says whether the disclosure container is emitted unconditionally"), and the spec forecloses that choice three separate ways. First, AC5 (spec:190-191) is a binding acceptance criterion:... |
| R8 | minor | newbreakage | S1 does not pin where cellKey sits in the metric record, so the extraction's existing key order is not guaranteed stable | The probe result is correct - `[ordered]@{name='n';id='i'}` then `.unit` then `.cellKey` yields key order name,id,unit,cellKey - but no reachable failure follows from either placement, and the finding concedes as much ("The array shape is safe either way"). I checked every consumer that could dep... |
| R9 | major | byteidentity | S6's disclosure container is undefined, so "only the canonicalVersion value differs" is not guaranteed | The finding's load-bearing sentence - that the spec never pins the no-flip behaviour, so an empty container would satisfy S6 and break AC5 - is contradicted by the spec text. spec:122-123 reads 'Such a report is byte-identical before and after, including its canonicalVersion-3 stamp being the onl... |
| R10 | minor | byteidentity | Rollout claims "such a report is byte-identical" but S1 changes every extraction file's bytes on every run | The mechanical half is true -- the metric record at Get-SwydoReport.ps1:516 flows into $out.metrics, into $widgetsOut, into $doc.widgets at :934, and is serialized at :940, so `cellKey` does add bytes to every extraction -- but the finding does not land as a defect in the spec. Three reasons. (1)... |
| R11 | minor | byteidentity | The i18n line claims no user-visible string is added, but S5 puts a raw metric id into breakdown labels | The stated failure mechanism does not exist. The finding says the uniquified key is "a raw provider metric id landing in the facts the report model renders breakdown table headers from" -- but skill/report-template.md forbids tables entirely: "**Narrate EVERY comparison as prose - never a table, ... |
| R12 | major | contract | AC5's byte-identity claim is unreachable unless S6 states the disclosure container is absent on a no-flip report | The spec already pins this twice over. S6 says the disclosure mirrors "the disclosure mechanism ratified for the U9 waiver", and that mechanism is per-CELL attachment with no container at all: Analyze-SwydoReport.ps1:767-768 attaches `canonical.supersededWidgetId` onto the headline cell itself, g... |
| R13 | major | contract | S5 puts a raw provider metric id into the label vocabulary the model narrates from, contradicting the §5 i18n claim | The mechanical facts hold — Get-SwydoReport.ps1:361 produces `"$base [$id]"`, pinned by Test-Extractor.ps1:36 as `Clicks [facebook-ads:clicks]` — but the spec already discloses this outcome rather than missing it: the §4 inventory row for `:401` states "second entry becomes the uniquified key" an... |
| R14 | minor | contract | AC7 names a statistic nothing in the repo can produce as written | The finding's central premise is wrong. It claims a "hit set" cannot exist without report text because tolerance is derived from the report token's ULP at Test-ReportNumbers.ps1:277. But every candidate carries its OWN `ulp` at construction time: Add-Candidate stores `[ordered]@{ value; type; has... |
| R15 | minor | contract | The extraction contract documents (governing spec + script header) are not in the Files-touched table | The line citations are accurate (SWYDO_REPORT_EXTRACTION_SPEC.md:296 metrics shape, :313 forward-compat, Get-SwydoReport.ps1:24 header), but the omission matches explicitly ratified shipped practice rather than contradicting it. EXTR-aRelativePeriod D10 (memory/extraction/builds/2026-07-13-EXTR-a... |
| R16 | minor | contract | The conditional skill/SKILL.md row resolves to 'no change' and can be settled now | The evidence is factually right and useless. `grep -c canonicalVersion skill/SKILL.md` does return 0, so the spec's two conditionals (:138, :177) resolve to "no change" -- which is exactly the outcome the spec's own wording produces when an implementer evaluates them. Nothing about the build diff... |

## Evidence for the confirmed findings

### C1 - S6's canonicalVersion 2->3 bump breaks two existing Test-Analyze.ps1 assertions the spec never lists

**Severity:** blocker - **lens:** claims - **spec section:** S6, §4 Files touched, AC8

**Evidence.** Test-Analyze.ps1:373 asserts `($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1)` ("e2e14/U9: meta.canonicalVersion=2 (D6 global bump)") and Test-Analyze.ps1:641 asserts `($t2.facts.meta.canonicalVersion -eq 2)` ("U9-T2: canonicalVersion=2 even on non-flip (D6 global)"). Both go red the moment `Analyze-SwydoReport.ps1:996` becomes `canonicalVersion=3`. The §4 Files-touched table lists Test-Analyze.ps1 as only "the NW helper and the S8 regression cases", and AC8 requires the suites to be "additive" — but these two are edits to existing assertions, not additions, so AC8 as written cannot be satisfied.

**Proposed fix.** Add to the Files-touched row for Test-Analyze.ps1: "update the two existing canonicalVersion=2 assertions at :373 and :641 to 3". Reword AC8 to "additive on Test-Analyze.ps1 and Test-Extractor.ps1, plus exactly two in-place assertion updates at Test-Analyze.ps1:373 and :641".

**Skeptic.** Verified in source. Test-Analyze.ps1:373 is `A ($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1) "e2e14/U9: meta.canonicalVersion=2 (D6 global bump), factsVersion=1"` and Test-Analyze.ps1:641 is `A ($t2.facts.meta.canonicalVersion -eq 2) "U9-T2: canonicalVersion=2 even on non-flip (D6 global)"`. S6 rewrites Analyze-SwydoReport.ps1:996 (`factsVersion=1; canonicalVersion=2`) to 3, so both assertions go red and §7's 'every leg green' claim fails until they are edited. The spec's §4 Files-touched row for Test-Analyze.ps1 reads only 'the NW helper and the S8 regression cases' — a known-red, mandatory in-place edit is unlisted, which is a real incompleteness in the change list. One correction to the finding's reasoning, which does not change the verdict: its secondary claim that 'AC8 as written cannot be satisfied' is a misreading. AC8 constrains suite COUNTS ('additive on Test-Analyze.ps1 and Test-Extractor.ps1 only, with the other six counts unchanged from 1064'), and editing two existing assertions in place leaves counts additive. Only the Files-touched half of the proposed fix is needed.

### C2 - canonicalVersion 2->3 makes the kickoff manifest's byte-identity trap stale; the manifest is not in Files touched

**Severity:** major - **lens:** claims - **spec section:** S6, §4 Files touched, §7 Gates

**Evidence.** .claude/SESSION-KICKOFF.md:167-170 reads: "The default single-report output path must stay byte-for-byte unchanged ... Sole exception: the reviewed, disclosed U9 flip-set waiver (D1/D3) - a zero-dim KPI superseding a doc-earlier table total changes the flipped cells (always disclosed in-facts) and bumps meta.canonicalVersion 1->2." S6 adds a second waiver and takes the stamp to 3, so both the "sole exception" clause and the "1->2" value become false. §4's Files-touched table does not list .claude/SESSION-KICKOFF.md, and §7 names the manifest ratchet only as a gate leg. AGENTS.md §1 Definition of Done requires re-stamping `last-audit` with a delta line when a unit changes what the manifest front-loads.

**Proposed fix.** Add a Files-touched row: `.claude/SESSION-KICKOFF.md | the byte-identity trap at :167-170 gains this unit's waiver and the stamp moves to 3; re-stamp last-audit per AGENTS.md §1`. Add it to the DoD checklist in §7.

**Skeptic.** .claude/SESSION-KICKOFF.md:167-170 reads exactly as quoted, including 'Sole exception: the reviewed, disclosed U9 flip-set waiver (D1/D3)' and 'bumps `meta.canonicalVersion` 1->2'. S6 adds a second disclosed waiver and moves the stamp to 3, so 'sole exception' becomes false and the trap no longer names the current stamp — a governing claim the next session front-loads about the project's load-bearing invariant. The spec's §4 Files-touched table lists only Get-SwydoReport.ps1, Analyze-SwydoReport.ps1, Test-Analyze.ps1, Test-Extractor.ps1 and skill/SKILL.md; §7 mentions the manifest only as a gate leg ('bash scripts/manifest-check.sh'). AGENTS.md §1 DoD requires updating the manifest when a unit makes one of its front-loaded claims stale, so this is an omitted mandatory edit. Partial mitigation, which does not refute it: the manifest watch list at .claude/SESSION-KICKOFF.md:6 covers `skill` and `Test-*.ps1`, so manifest-check.sh C5 ('no unaudited watch drift') will mechanically force the last-audit re-stamp. It does not and cannot fix the stale trap prose at :167-170 — manifest-check verifies stamps and pathspecs, not claim truth.

### C3 - The defect characterization and AC5's byte-identity precondition ignore case-insensitive collision

**Severity:** major - **lens:** claims - **spec section:** §4 Data model, §4 Rollout, AC5

**Evidence.** §4 Data model says the extractor and analyzer "disagree exactly when a widget carries two metrics with the same display name, or one with an empty display name", and AC5/Rollout condition byte-identity on names being "non-empty and unique within that widget". Both are wrong on case. Uniq-Key's collision test at Get-SwydoReport.ps1:360 is `$map.Contains($base)` against the `[ordered]@{}` built at `:524`, an OrderedDictionary with case-insensitive keys. Verified under PowerShell 5.1: `Uniq-Key` for ('Clicks','google-adwords:clicks',0) then ('clicks','google-adwords:link_clicks',1) returns `Clicks` and `clicks [google-adwords:link_clicks]` — the second is suffixed, so cellKey != $m.name and the report is NOT byte-identical despite the names being distinct strings. The same probe confirms today's defect on that pair: after a JSON round-trip, `$row.metrics.'clicks'` returns metric 1's value, because PowerShell member access is case-insensitive too.

**Proposed fix.** In §4 Data model, change "two metrics with the same display name" to "two metrics whose display names are equal ignoring case (Uniq-Key's map is a case-insensitive OrderedDictionary)". In AC5 and §4 Rollout, change "non-empty and unique within that widget" to "non-empty and unique within that widget under ordinal-ignore-case comparison". Add a case-variant pair to the S7/AC4 parity fixtures.

**Skeptic.** Reproduced under PowerShell 5.1 (5.1.22621.4249) against the real Uniq-Key body from Get-SwydoReport.ps1:358-364 with the same `[ordered]@{}` map the extractor builds at :524. Calling Uniq-Key for ('Clicks','google-adwords:clicks',0) then ('clicks','google-adwords:link_clicks',1) returns `Clicks` and `clicks [google-adwords:link_clicks]` — the second IS suffixed, because :360's `$map.Contains($base)` runs against an OrderedDictionary with a case-insensitive comparer. So cellKey != $m.name for a pair whose display names are non-empty and distinct as strings, and AC5 / §4 Rollout ('non-empty and unique within that widget') state a precondition that does not actually imply byte-identity. That matters more than usual here: the byte-identity claim is what the spec offers in place of a dark rollout ('provable rather than measured'), so a wrong precondition undermines the review basis for the waiver. The same probe also confirms §4 Data model understates the defect: after a JSON round-trip, `$row.metrics.'clicks'` returned 50 (metric 1's value) rather than 900, because PSObject member access — and therefore Row-Cur/Row-Cmp at Analyze-SwydoReport.ps1:192-193 — is case-insensitive too. 'Disagree exactly when a widget carries two metrics with the same display name' is thus false; case-variant names diverge as well.

### C4 - Repaired empty-display-name metrics publish an EMPTY metric label into the headline and WIN/LOSS prose

**Severity:** major - **lens:** claims - **spec section:** AC2, §4 Inventory, §5 i18n

**Evidence.** AC2 only asserts the metric "appears in the headline and GAP_NO_ACCOUNT_TOTAL does not name it". But the headline record built at Analyze-SwydoReport.ps1:758-759 stores `metric=$m.name`, which for the empty-display-name fixture is the empty string. That field is consumed verbatim at `:905` — `statement="$($pf.name) $($h.metric) $($h.displayCurrent) vs ..."` — yielding `Google Ads 900 vs 700 (+28.6%)` with a doubled space and no metric, and at `:899` for GAP_UNIT_UNCONFIRMED. skill/report-template.md:34 then has the model write `- <Metric> was/were <displayCurrent> ...` from that same field. Today the metric is silently dropped; after the repair it ships nameless into the client report. `:759` is absent from the inventory table.

**Proposed fix.** Add `:759` to the inventory (`headline metric label | :759 | metric absent | metric labelled with the resolved cellKey`). Extend S3 to set `metric=` to the resolved key rather than `$m.name` — a no-op for every non-empty unique name, so the AC5 byte-identity argument is preserved. Extend AC2 to assert the surfaced headline cell and its WIN/LOSS statement carry a non-empty label equal to the resolved key.

**Skeptic.** Traced in source. S3 repoints the total-row LOOKUP at Analyze-SwydoReport.ps1:738, but the headline record built at :758-759 stores `metric=$m.name`, which the spec leaves untouched — for the empty-display-name fixture that is the empty string (or null). Today the metric never reaches that line because the :738 lookup misses and :739 `continue`s; after the repair the cell resolves and the record is written with an empty label. The consumers are real: :905 builds `statement="$($pf.name) $($h.metric) $($h.displayCurrent) vs ..."`, and :899 does the same for GAP_UNIT_UNCONFIRMED. Reachability of the WIN/LOSS path for the spec's own fixture holds: Get-Direction at :58-59 matches 'impression' to 'higher-better', so the :900 direction guard passes. skill/report-template.md:34 then renders '- <Metric> was/were <displayCurrent> ...' from that field. AC2 as written ('appears in the headline and GAP_NO_ACCOUNT_TOTAL does not name it') is satisfied by output that ships a nameless metric to the client, so the spec is incomplete in a way that changes the build. The proposed fix is sound and preserves the byte-identity argument: for every non-empty name unique within its widget the resolved key equals $m.name, so setting `metric=` to the resolved key is a no-op there.

### C5 - AC8's suite-count arithmetic is wrong: the other six suites total 366, not 1064

**Severity:** minor - **lens:** claims - **spec section:** AC8

**Evidence.** AC8 requires "the other six counts unchanged from 1064 in total". Measured under PowerShell 5.1 at this tree: Test-Analyze 467, Test-Archive 94, Test-Closer 129, Test-Extractor 231, Test-Ledger 47, Test-Sync 4, Test-TrendAnalyze 68, Test-TrendFacts 24 — 1064 across all EIGHT. The six suites this unit does not touch total 366. As written AC8 is unsatisfiable. (The 1064 figure quoted in S9 and §5 as the overall green baseline is correct.)

**Proposed fix.** Reword AC8: "the six untouched suites hold at 366 in total (Archive 94, Closer 129, Ledger 47, Sync 4, TrendAnalyze 68, TrendFacts 24), and the eight-suite total rises above 1064 by exactly the S7 + S8 additions."

**Skeptic.** Measured independently under PowerShell 5.1 at this tree, running each suite in a child powershell.exe: Test-Analyze 467, Test-Archive 94, Test-Closer 129, Test-Extractor 231, Test-Ledger 47, Test-Sync 4, Test-TrendAnalyze 68, Test-TrendFacts 24, all exit 0. Sum across all eight = 1064. Sum of the six this unit does not touch (Archive 94 + Closer 129 + Ledger 47 + Sync 4 + TrendAnalyze 68 + TrendFacts 24) = 366. Every number in the finding matches to the unit. AC8 (spec:196-198) attaches the 1064 figure to 'the other six counts unchanged from 1064 in total'. Under its natural reading that is arithmetically false and the criterion cannot be satisfied. The finding is also right that the 1064 used at S9 (spec:38) and in the testing bullet (spec:174) as the overall green baseline is correct, so the defect is localized to AC8 rather than a systemic miscount. This is an acceptance criterion, so a wrong number changes what the implementer verifies and reports at the merge bar, and the proposed rewrite supplies the verified per-suite figures. Minor in severity but concrete, measured, and actionable against the spec text.

### C6 - Byte-identity precondition is false: display names differing only in case are a third, un-enumerated flip class

**Severity:** blocker - **lens:** completeness - **spec section:** section 4 Rollout + AC5 (and the Inventory reachability paragraph, lines 98-100)

**Evidence.** Rollout (spec:119-124) claims: "for every widget whose metric display names are non-empty and unique within that widget, cellKey equals $m.name at every one of the sites". AC5 (spec:190-191) uses the same precondition. That is false, because Uniq-Key's collision test is Contains() on an OrderedDictionary: Get-SwydoReport.ps1:360 `if(-not $map.Contains($base)){ return $base }` against the `$mmap=[ordered]@{}` built at Get-SwydoReport.ps1:524, which is case-insensitive, while the analyzer's lookup `$row.metrics.$name` (Analyze-SwydoReport.ps1:192-193, :738, :931) is also case-insensitive. Reproduced under PowerShell 5.1 against the real Uniq-Key: metrics displayed `Clicks` (google-adwords:clicks, cell 100) and `clicks` (google-adwords:link_clicks, cell 200) produce extractor keys `Clicks | clicks [google-adwords:link_clicks]`, and `$row.metrics.'clicks'` returns 100 -- metric 1's value under metric 2's identity. Both names are non-empty and are distinct strings, so this widget satisfies the spec's stated precondition yet is NOT byte-identical after the repair. The Inventory's reachability paragraph (spec:98-100) enumerates only two divergences (`col<idx>` and empty-name) and says "Both diverge from $m.name", closing the set at two.

**Proposed fix.** Reword the Rollout claim and AC5 to "non-empty and unique CASE-INSENSITIVELY (OrderedDictionary key equality) within that widget". Add a third sentence to the Inventory reachability paragraph naming the case-collision class and citing Get-SwydoReport.ps1:360 + :524 as the reason `Clicks`/`clicks` collide. Extend S8 with a third fixture (two metrics displayed `Clicks` and `clicks`) and add an AC mirroring AC1 for it. Extend S7's parity assertion to include a case-differing metric list, so the drift gate pins the comparer semantics rather than assuming ordinal.

**Skeptic.** Reproduced verbatim under PowerShell 5.1 against the real Uniq-Key and the real Normalize-Widget at this tree. Two metrics displayed 'Clicks' (google-adwords:clicks) and 'clicks' (google-adwords:link_clicks), total-row cells 100 and 200, produce row-map keys 'Clicks' and 'clicks [google-adwords:link_clicks]'. The collision fires because Uniq-Key's test is $map.Contains($base) (Get-SwydoReport.ps1:360) against the [ordered]@{} built at :524, and OrderedDictionary key equality is case-insensitive. On the analyzer side the lookup is equally case-insensitive: $total.metrics.'clicks'.current returns 100 and Row-Cur $total 'clicks' returns 100 -- metric 1's value under metric 2's identity, exactly the shipped defect class. That widget satisfies the spec's stated precondition and still flips. Rollout (spec:119-124) claims 'for every widget whose metric display names are non-empty and unique within that widget, cellKey equals $m.name at every one of the sites', and AC5 (spec:190-191) repeats it. 'Clicks' and 'clicks' are both non-empty and are distinct strings, yet cellKey for the second is 'clicks [google-adwords:link_clicks]', not $m.name. The universally-quantified claim is therefore false, and it is the load-bearing one: the spec explicitly offers this provable byte-identity claim as its substitute for landing dark (spec:115-124), so a single counterexample defeats the risk control, independent of how often case-differing names occur in the wild. The established duplicate-name defect already proves widgets carry near-duplicate display names. This changes the build in four concrete places named in the fix: the Rollout wording and AC5 must say unique CASE-INSENSITIVELY, S8 needs a third fixture, a new AC must mirror AC1 for it, and S7's parity assertion must include a case-differing metric list so the drift gate pins comparer semantics rather than silently assuming ordinal. One caveat on the evidence: the Inventory paragraph at spec:98-100 is enumerating the two EMPTY-name sub-cases (col<idx> vs id fallback), so 'closes the set at two' overreads it slightly -- but the same gap exists in the Data model sentence at spec:60-62 ('two metrics with the same display name'), and the Rollout/AC5 defect stands on its own.

### C7 - Inventory omits the native-ratio reconciliation sites at :528 and :545

**Severity:** major - **lens:** completeness - **spec section:** section 4 Inventory table (row "reconciliation rules") and S4

**Evidence.** The Inventory's "reconciliation rules" row names only `:593`, `:595`, `:639`, `:647`. Get-RatioReconFindings has three more display-name cell reads that the table never lists: Analyze-SwydoReport.ps1:528 `$unitOk = ($null -ne (Row-Cur $tr $rm.name))` (the roas admission gate) and :545 `$numv=Row-Cur $tr $num.name; $denv=Row-Cur $tr $den.name; $repv=Row-Cur $tr $rm.name`. These feed DISC_RATIO_UNIT (severity major, :564) and DISC_RATIO_RECOMPUTE (:570), whose `statement` and `evidence` carry recomputed numbers, and report-template.md:21 forces every discrepancy at severity >= major into the client report. S4's catch-all ("every Row-Cur and Row-Cmp call site") scopes them in, but the Inventory is what the spec designates as the flip set -- it is the basis for S6's disclosure and for AC5/AC6/AC7 -- so a site absent from it is a surface that flips with no measurement and no acceptance criterion. The flip is real in both directions: with a duplicate display name on the reported ratio, :545 recomputes against metric 1's cell and emits a wrong major discrepancy; with an empty display name on a component, :528/:545 return $null today and the check silently skips, so the repair NEWLY fires findings that do not exist at HEAD.

**Proposed fix.** Add an Inventory row: `| native-ratio reconciliation | :528, :545 | reported ratio and its components read the first same-named cell; an empty-named component makes the whole check skip | each component and the reported ratio read their own cell |`. Name `:528` and `:545` explicitly in S4 alongside the other reconciliation sites, and add an AC that a duplicate-name ratio fixture emits DISC_RATIO_* with each metric's own numbers (or emits none, whichever the fixture warrants) before and after.

**Skeptic.** Verified against source. `grep -n 'Row-Cur\|Row-Cmp' Analyze-SwydoReport.ps1` returns 23 sites; the Inventory's nine rows cover :738, :806-828, :904-907, :397, :401, :931, :593, :595, :639, :647, :219-281, :384, :436, :453-457, :853. Lines 528 (`$unitOk = ($null -ne (Row-Cur $tr $rm.name))`) and 545 (`$numv=Row-Cur $tr $num.name; $denv=Row-Cur $tr $den.name; $repv=Row-Cur $tr $rm.name`) fall in NO inventory row. I tried to refute this as a restatement of S4's catch-all -- and the code change genuinely IS already scoped in by S4, so the builder's edit does not change. What survives is the second leg, which is real and independent: at :546 the check does `if($null -eq $numv -or $null -eq $denv -or $null -eq $repv ...){ continue }`, so an empty-named component makes Get-RatioReconFindings skip silently at HEAD; after the repair the cell resolves and the check runs, so DISC_RATIO_UNIT (severity='major', :564) and DISC_RATIO_RECOMPUTE (:570) can fire where they do not fire today. report-template.md:21 forces every discrepancy at sev >= major into the client report. That is a NEW client-facing finding surface with no AC and no inventory row, and the Rollout section (spec:119-124) rests its byte-identity claim on `cellKey` equalling `$m.name` 'at every one of the sites in the inventory' -- a claim whose scope the omission understates. Changes the build's test plan and disclosure scope, so it stands.

### C8 - Metrics with an empty display name are un-dropped into a client-facing surface with a blank label

**Severity:** major - **lens:** completeness - **spec section:** S3 / Inventory row "headline presence" / AC2 / section 5 i18n

**Evidence.** AC2 (spec:184-185) requires only that the empty-named metric "appears in the headline and GAP_NO_ACCOUNT_TOTAL does not name it". Nothing in the spec addresses the label it appears under. Analyze-SwydoReport.ps1:759 stores `metric=$m.name` verbatim into the headline record, so the repaired metric enters the headline with `metric=""`. That value then flows to Analyze-SwydoReport.ps1:904-906, where the WIN/LOSS record is built as `metric=$h.metric` with `statement="$($pf.name) $($h.metric) $($h.displayCurrent) vs $($h.displayPrevious) ($($h.displayDelta))"` -- yielding `"Google Ads 900 vs 720 (+25.0%)"`, a double space where the metric name belongs. The same empty `$h.metric` reaches GAP_UNIT_UNCONFIRMED at :899. Today this cannot happen, because the empty-named metric never gets a headline cell at all (:738-739). The repair therefore introduces a new client-visible defect on exactly the fixture the spec calls a "first-class fixture" (spec:165-166), and section 5's i18n bullet (spec:163-164) asserts "No user-visible string is added", which is not true of an empty one.

**Proposed fix.** Add an S-item: wherever a metric's display name is emitted as a LABEL rather than used as a lookup -- Analyze-SwydoReport.ps1:759 `metric=`, and by inheritance :899 and :904-906 -- fall back to the resolved cell key when `$m.name` is null/empty, so the label is `google-adwords:impressions` rather than blank. Extend AC2 to assert the emitted headline `metric` and the WIN/LOSS `statement` for the empty-named metric contain a non-empty label, and delete or qualify the "No user-visible string is added" claim in section 5.

**Skeptic.** Verified end to end. S3 repoints only the LOOKUP at :738 and S4 only Row-Cur/Row-Cmp arguments; nothing in the spec touches Analyze-SwydoReport.ps1:759 `metric=$m.name`, which is a LABEL write, not a lookup. So the repaired empty-named metric enters the headline record with `metric=""`. That value is read at :899 (GAP_UNIT_UNCONFIRMED `metric=$h.metric`) and at :904-906, where `statement="$($pf.name) $($h.metric) $($h.displayCurrent) vs $($h.displayPrevious) ($($h.displayDelta))"` renders as `Google Ads 900 vs 720 (+25.0%)` -- a double space where the metric name belongs. I checked reachability rather than assuming it: today :738-739 `$cell = $tr.metrics.$($m.name)` with an empty name yields $null and `if(-not $cell){ continue }` drops the metric, so this string cannot be produced at HEAD; the repair is what makes it reachable. WIN/LOSS is client-facing per report-template.md:56-57, and no closer check touches `.metric` (Test-ReportNumbers.ps1 traces display strings and fids). AC2 (spec:184-185) asserts only presence in the headline and absence from GAP_NO_ACCOUNT_TOTAL, and section 5's i18n bullet (spec:163-164) asserts 'No user-visible string is added', which the blank label contradicts. A real new client-visible defect on the spec's own first-class fixture.

### C9 - F4's "no report-template change" is unsupported: the uniquified key becomes the client-facing breakdown label, and it is an internal id

**Severity:** major - **lens:** completeness - **spec section:** F4 resolution (spec:231-235), S5, section 5 i18n, Inventory row "breakdown labels"

**Evidence.** S5 and the Inventory (spec:92-93) make the breakdown key and `metricNames` hold the uniquified key: `Conversions [google-adwords:all_conversions]` for the duplicate case, and the bare metric id `google-adwords:impressions` for the empty-name case. Analyze-SwydoReport.ps1:397 `$vals[[string]$m.name]=$cell` and :401 `metricNames=@($mets|ForEach-Object{$_.name})` are the ONLY place the facts document names a breakdown metric -- there is no separate label field. report-template.md:3 directs the model to take breakdown numbers from `breakdowns[].rows[].values[].display`, report-template.md:40 asks for "a short prose call-out of a key breakdown row", and report-template.md:78 states "Keep the client copy free of internal ids, share links, and the facts filename." So the repair routes a raw provider metric id, or a name with a bracketed id appended, into the only string the model has to name that metric in client prose -- which is a report-voice consequence, exactly the thing F4 says it is avoiding. Section 5's i18n bullet defends this as "the uniquified key is already the extraction's own vocabulary", which is true of the extraction and false of the delivered report. Note the closer is genuinely unaffected: Test-ReportNumbers.ps1:224-228 iterates `$row.values.PSObject.Properties.Name` and never interprets the key, so the spec's non-goal about the closer holds for value tracing.

**Proposed fix.** Either (a) add to S5 an additive sibling field per breakdown cell -- e.g. `values[].<key>.label` and a parallel `metricLabels[]` -- carrying the human display name (falling back to the metric id only when the name is empty), leaving the key as the identity; or (b) reopen F4 and add a one-line rule to skill/report-template.md that a breakdown key of the form `<name> [<id>]` is narrated as `<name>` and a key that is a bare `provider:metric` id is narrated from the headline's label, never printed verbatim. Whichever is chosen, add report-template.md to the Files-touched table if (b), and add an AC asserting no client-facing label is a bare provider metric id.

**Skeptic.** I looked for a reason to call this taste and did not find one. `grep -rn metricNames` returns exactly one producer, Analyze-SwydoReport.ps1:401, and ZERO consumers outside the spec -- so the breakdown key at :397 and `metricNames` at :401 are a pure model-facing label surface with no validator. I confirmed the finding's own concession is correct: Test-ReportNumbers.ps1:224-228 iterates `$row.values.PSObject.Properties.Name` and never interprets the key, so value tracing is unaffected AND nothing in the closer would block an internal id reaching the client copy. Against that, report-template.md:3 points the model at `breakdowns[].rows[].values[].display`, :40 asks for 'a short prose call-out of a key breakdown row', and :78 states 'Keep the client copy free of internal ids, share links, and the facts filename.' After S5 the empty-name key is a bare `google-adwords:impressions` and the duplicate key is `Conversions [google-adwords:all_conversions]`. Section 5's defence ('the uniquified key is already the extraction's own vocabulary') is true of rows[].metrics and false of the facts-to-report boundary this unit newly routes it through. report-template.md is absent from the Files-touched table and AC3 constrains only that metricNames holds 'two distinct entries', not that either is client-safe. A hard template rule violated with no gate is not a matter of taste.

### C10 - S2 defines no metric-object-to-key mapping, and :647 resolves a metric belonging to a different widget

**Severity:** major - **lens:** completeness - **spec section:** S2, section 4 Data model, S4

**Evidence.** S2 says the analyzer "reads $m.cellKey when present and otherwise replays Uniq-Key over widget.metrics[]", and section 4 says it "resolves one lookup key per metric per widget". Neither says how a replayed key is associated back to a metric OBJECT, and most call sites hold only an object with no index: Analyze-SwydoReport.ps1:129 `function Find-Metric($w,$pat){ ... return $m }` supplies `$em`/`$rm` at :246-252, `$om` at :271-281, `$money`/`$leadM`/`$convM` at :424-428 (used at :436), and `$rmet` at :865; `$primary` is selected by a foreach at :218/:448-450 (used at :219-239, :384, :453-457); `$num`/`$den`/`$rm` at :521-534 (used at :528, :545). An id-keyed map is not a sufficient association: Uniq-Key's third tier at Get-SwydoReport.ps1:363 `return "$base [$id #$idx]"` exists precisely for two metrics sharing BOTH name and id, so `$w.metrics` may contain two records with identical id. Separately, :647 `$ov=Row-Cur $otr $om[0].name` reads `$om[0]`, which comes from `$ow.metrics` at :646 -- a DIFFERENT widget from the `$w` under analysis -- so a resolution scoped to "the widget under analysis" resolves the wrong key there.

**Proposed fix.** Make S2 prescriptive: immediately after the schema gate at Analyze-SwydoReport.ps1:678, walk every data widget and every `$w.metrics[$i]` by INDEX and stamp `cellKey` onto the record (Add-Member when the extraction predates S1, replaying Uniq-Key with `$i`), so `$m.cellKey` is present on every metric record of every widget for the rest of the run. Then S4 becomes mechanical -- each site substitutes `$m.name` with `$m.cellKey` -- and :647 is correct by construction because `$om[0]` was stamped from its own widget. State explicitly that an id-keyed or name-keyed resolution map is forbidden, citing Get-SwydoReport.ps1:363.

**Skeptic.** I pushed hard on refuting this as terseness rather than a gap, and one leg survived cleanly. Get-SwydoReport.ps1:363 `return "$base [$id #$idx]"` is the third Uniq-Key tier and exists precisely for two metrics sharing BOTH name and id, so an id-keyed resolution map is provably insufficient -- and a name-keyed one is the defect itself. S2's fallback wording ('replays Uniq-Key over widget.metrics[]') yields a SEQUENCE, while every consuming site holds a bare object with no index: `$primary` from the foreach at :218 and :448-450, `$em`/`$rm` from Find-Metric at :246, `$om` at :271, `$money`/`$leadM`/`$convM` at :424-428, `$rmet` at :865, `$num`/`$den`/`$rm` at :531-534. The spec never states the association mechanism, and the two obvious shortcuts both fail. The cross-widget leg checks out too: :646 `$om=@(@($ow.metrics) | Where-Object { $_.id -eq $m.id })` and :647 `$ov=Row-Cur $otr $om[0].name` operate on `$ow`/`$otr`, a different widget from the `$w` under analysis, while section 4 says only 'one lookup key per metric per widget' -- an implementation that resolves a map for `$w` and applies it at :647 reads the wrong widget's keys. The proposed index-stamp after the schema gate at :678 makes S4 mechanical and :647 correct by construction, which changes how the build is done.

### C11 - Test flip set omits two in-place canonicalVersion assertions; AC8's "additive only" is false as written

**Severity:** major - **lens:** completeness - **spec section:** S6, section 4 Files touched (Test-Analyze.ps1 row), AC8

**Evidence.** S6 bumps `meta.canonicalVersion` 2 -> 3 at Analyze-SwydoReport.ps1:996. Two existing assertions pin the old value and will go RED, not merely stay flat: Test-Analyze.ps1:373 `A ($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1) "e2e14/U9: meta.canonicalVersion=2 (D6 global bump), factsVersion=1"` and Test-Analyze.ps1:641 `A ($t2.facts.meta.canonicalVersion -eq 2) "U9-T2: canonicalVersion=2 even on non-flip (D6 global)"`. The Files-touched table (spec:136) lists Test-Analyze.ps1 as only "the NW helper and the S8 regression cases", and AC8 (spec:196-198) asserts the suites are "additive on Test-Analyze.ps1 and Test-Extractor.ps1 only". The precedent this spec leans on handled it explicitly: memory/analysis/builds/2026-07-13-ANLZ-aHeadlineRank/spec/2026-07-13-spec-aHeadlineRank-1.md:168 budgets "two documented in-place assertion updates (e2e11 -> new-rank pin, e2e14 -> canonicalVersion 2)". Grep confirms no other suite pins canonicalVersion, so the blast radius is exactly these two lines.

**Proposed fix.** Change the Files-touched Test-Analyze.ps1 row to "the NW helper, the S8 regression cases, and two documented in-place assertion updates at :373 and :641 (canonicalVersion 2 -> 3)". Reword AC8 to "additive on Test-Analyze.ps1 and Test-Extractor.ps1, plus exactly two in-place assertion edits at Test-Analyze.ps1:373 and :641; the other six suite counts unchanged".

**Skeptic.** Purely mechanical and grep-verified, no judgment needed. A repo-wide `grep -rn canonicalVersion` over .ps1/.md/.json shows exactly one writer (Analyze-SwydoReport.ps1:996 `canonicalVersion=2`) and exactly two assertions: Test-Analyze.ps1:373 `A ($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1) "e2e14/U9: ..."` and Test-Analyze.ps1:641 `A ($t2.facts.meta.canonicalVersion -eq 2) "U9-T2: canonicalVersion=2 even on non-flip (D6 global)"`. No .json fixture pins it and no other suite references it, so the blast radius is exactly those two lines and both go RED under S6's 2 -> 3 bump. The Files-touched row (spec:136) budgets Test-Analyze.ps1 for 'the NW helper and the S8 regression cases' only, and AC8 (spec:196-198) asserts the suites are 'additive on Test-Analyze.ps1 and Test-Extractor.ps1 only' -- an in-place assertion edit is not additive, so AC8 is false as written and contradicts S6. The precedent the spec leans on handled this explicitly: memory/analysis/builds/2026-07-13-ANLZ-aHeadlineRank/spec/2026-07-13-spec-aHeadlineRank-1.md:168 budgets 'two documented in-place assertion updates'. Nothing in the spec acknowledges either line.

### C12 - Doc flip set omits the two governing records that assert the U9 waiver is the SOLE canonicalVersion exception, and the manifest re-stamp is unbudgeted

**Severity:** minor - **lens:** completeness - **spec section:** section 4 Files touched, section 5 user docs, section 7 Gates

**Evidence.** Two tracked records state the current waiver is unique and will be falsified by S6. memory/analysis/DECISIONS.md:8: "ANLZ-aGovernedCanon-3 - the default single-report output stays byte-for-byte unchanged ... Sole exception: the reviewed, disclosed U9 flip-set waiver, which bumps `meta.canonicalVersion` 1->2". .claude/SESSION-KICKOFF.md:167-170 repeats it: "Sole exception: the reviewed, disclosed U9 flip-set waiver (D1/D3) ... bumps `meta.canonicalVersion` 1->2." The Files-touched table lists neither, and section 5's user-docs bullet scopes doc work to skill/SKILL.md alone (which in fact contains no canonicalVersion reference at all -- grep returns nothing, so that conditional row resolves to no work). Independently, the manifest watch list at .claude/SESSION-KICKOFF.md:6 is `AGENTS.md; skill; tools; scripts; memory-tree; memory-recall; .memory-tree.conf; Test-*.ps1`; this unit touches `skill` and `Test-*.ps1`, so `bash scripts/manifest-check.sh` check C5 ("no unaudited watch drift", scripts/manifest-check.sh:6) requires a `last-audit` re-stamp that the Files-touched table does not budget -- and section 7 lists the manifest ratchet as a gate that must be green.

**Proposed fix.** Add two rows to the Files-touched table: `.claude/SESSION-KICKOFF.md` -- correct the "Sole exception" clause at :167-170 to name both waivers and re-stamp `last-audit` with a delta line per AGENTS.md section 1; and `memory/analysis/DECISIONS.md` -- append a superseding decision line (the log is append-only per AGENTS.md section 6, so do not rewrite ANLZ-aGovernedCanon-3) recording that ANLZ-aCandidTally-1 is the second disclosed waiver taking canonicalVersion 2 -> 3.

**Skeptic.** Every cited fact holds. memory/analysis/DECISIONS.md:8 reads "Sole exception: the reviewed, disclosed U9 flip-set waiver, which bumps `meta.canonicalVersion` 1->2" and .claude/SESSION-KICKOFF.md:167-170 repeats it verbatim as a trap. Spec S6 (spec:27-30) creates a SECOND waiver taking 2->3, so both records become false the moment this unit lands, and the spec's Files-touched table (spec:130-138) lists neither file. The manifest half is mechanically enforceable, not advisory: watch at .claude/SESSION-KICKOFF.md:6 is `AGENTS.md; skill; tools; scripts; memory-tree; memory-recall; .memory-tree.conf; Test-*.ps1`, this unit stages `skill/scripts/*.ps1` and `Test-Analyze.ps1`/`Test-Extractor.ps1`, and .githooks/pre-commit lines 26-29 run `manifest-check.sh --staged`, whose C5s leg (scripts/manifest-check.sh:204-219) FAILS the commit unless the staged manifest's last-audit value differs from HEAD's. The full C5 leg (scripts/manifest-check.sh:182-201) is wired into the standing bar at tools/gate-legs.json:19, which spec section 7 requires green. So the manifest edit is a mandatory part of this unit's commit and the spec does not budget it. Worse, last-audit is defined at .claude/SESSION-KICKOFF.md:29 as "an assertion of verification" — a bare re-stamp would certify a section-B trap line this unit just falsified. The SKILL.md sub-claim also verifies: grep for canonicalVersion in skill/SKILL.md and skill/report-template.md returns nothing, so the spec's one conditional doc row (spec:138) resolves to zero work and the whole doc-flip set is empty as written. Actionable and build-changing: two rows added to the Files-touched table, one append-only superseding line in the decision index, one corrected trap clause plus the re-stamp.

### C13 - S2's replay map has no pinned comparer, and a case-sensitive one silently re-creates the exact defect this unit fixes

**Severity:** blocker - **lens:** newbreakage - **spec section:** S2 (spec:18-20), S7 (spec:31-33), AC4 (spec:188-189)

**Evidence.** skill/scripts/Get-SwydoReport.ps1:360 - Uniq-Key's collision test is `$map.Contains($base)`, and the map it is handed at :525 is `[ordered]@{}`, i.e. a case-INSENSITIVE OrderedDictionary. So the extractor's key set is unique case-insensitively. S2 says only "replays Uniq-Key over widget.metrics[]" and never pins the container the analyzer feeds it. The analyzer's own established idiom for a seen-set is case-SENSITIVE: Analyze-SwydoReport.ps1:813 uses `[System.Collections.Generic.HashSet[string]]::new()`, which exposes .Contains and would drop straight into Uniq-Key's body unchanged. I reproduced the divergence against HEAD under PS 5.1 driving the real Normalize-Widget: metrics ('Conversions', google-adwords:conversions) and ('conversions', google-adwords:all_conversions), cells 100 and 250. Extractor row keys: `Conversions | conversions [google-adwords:all_conversions]`. A case-sensitive replay derives `Conversions | conversions`. Looking both derived keys up on the JSON round-tripped row returns current=100 for BOTH, because PSCustomObject member access is case-insensitive so the wrong key resolves to a real cell instead of $null. That is metric 2 publishing metric 1's value - the defect, restored, through the repair. AC4/S7 would not catch it: the parity fixture is unspecified, and Test-Extractor.ps1:33-39's existing Uniq-Key cases are all exact-case.

**Proposed fix.** Amend S2 to mandate the fallback map be `[ordered]@{}` (or `@{}`), byte-for-byte the container the extractor uses at Get-SwydoReport.ps1:525, and to forbid HashSet[string]/any explicit ordinal comparer. Amend S7 and AC4 to require the parity fixture to include a case-only-differing display-name pair ('Conversions'/'conversions' with distinct ids) plus the empty-name and same-name-same-id cases, so the gate actually exercises every Uniq-Key branch rather than the happy path.

**Skeptic.** Reproduced under PowerShell 5.1 against HEAD driving the real Uniq-Key. Get-SwydoReport.ps1:360 tests `$map.Contains($base)` and the map handed to it at :525 is `[ordered]@{}`. With metrics ('Conversions', google-adwords:conversions) and ('conversions', google-adwords:all_conversions) the extractor derives `Conversions` and `conversions [google-adwords:all_conversions]`. Handing the SAME function a `[System.Collections.Generic.HashSet[string]]::new()` — which exposes .Contains and is the analyzer's own established seen-set idiom at Analyze-SwydoReport.ps1:813 — derives `Conversions` and `conversions`. Looking the second derived key up on the JSON round-tripped row returns 100, not 250, because PSCustomObject member access is case-insensitive so the wrong key resolves to metric 1's real cell instead of $null. That is metric 2 publishing metric 1's value: the defect, restored through the repair, silently. Spec S2 (spec:18-20) says only "replays `Uniq-Key` over `widget.metrics[]`" and never pins the container; section 4's byte-identity argument (spec:119-124) leans entirely on the replay reproducing the extractor sequence exactly. The claimed gate escape also holds: AC4 (spec:188-189) names no fixture content, and Test-Extractor.ps1:33-39's existing Uniq-Key cases use exact-case duplicates ('Clicks'/'Clicks'), which a case-sensitive comparer suffixes identically and therefore passes. Pinning the container in S2 and pinning the parity fixture's contents in S7/AC4 both change what gets built.

### C14 - S2 does not say WHERE the fallback runs, and the 135 -DefineOnly fixtures build metric records with no cellKey

**Severity:** major - **lens:** newbreakage - **spec section:** S2 (spec:18-20), S9 (spec:36-38)

**Evidence.** Every -DefineOnly unit fixture in Test-Analyze.ps1 builds metrics through `function Met($n,$id,$unit=$null){ [pscustomobject]@{name=$n;id=$id;unit=$unit} }` at Test-Analyze.ps1:142 - no cellKey, ever - and calls the pure helpers directly (Get-BreakdownFindings, Get-Breakdown, Get-TimeSeries, Get-DetailSumFindings, Get-SliceAccountFindings). Those helpers are also the ones S4/S5 repoint. If the resolution pass runs once in the run body (after the ConvertFrom-Json at Analyze-SwydoReport.ps1:676), every direct-helper unit test gets `$m.cellKey` = $null, and per the silent-null behaviour of `$row.metrics.$null` those helpers return empty rather than failing usefully. If instead the pure helpers resolve internally, the same fixtures keep working through the fallback. The spec picks neither, and S9 only adds an `NW` helper for the NEW cases - it leaves the 467 existing Test-Analyze assertions riding on `Met`.

**Proposed fix.** Amend S2 to state that key resolution is a pure per-widget helper invoked INSIDE each rule function that reads cells (idempotent, cheap, no run-body pass), so a widget assembled by `Met`/`DW` without cellKey resolves through the replay exactly as an old extraction does. Add to AC8 that Test-Analyze's existing 467 assertions stay green without editing the `Met` helper at :142.

**Skeptic.** The fixture facts verify exactly. Test-Analyze.ps1:142 is `function Met($n,$id,$unit=$null){ [pscustomobject]@{name=$n;id=$id;unit=$unit} }` — no cellKey, and it is used 137 times; Test-Analyze.ps1:306 states in the suite's own words "The 135 cases above are -DefineOnly unit tests that never exercise the run body"; and there are 33 direct calls to the very helpers S4/S5 repoint (Get-BreakdownFindings, Get-Breakdown, Get-TimeSeries, Get-DetailSumFindings, Get-SliceAccountFindings), all reached through the -DefineOnly dot-source at Test-Analyze.ps1:7 which returns at Analyze-SwydoReport.ps1:669, before the run body. S2 (spec:18-20) genuinely picks neither location. The consequence is material and asymmetric: a run-body pass after the ConvertFrom-Json at :676 leaves `$m.cellKey` $null in every one of those fixtures (I confirmed Row-Cur returns $null on a null key), so the repointed helpers return empty and the existing assertions must be rewritten or `Met` amended; a resolver invoked inside each rule function leaves them all working untouched. S9 (spec:36-38) only adds an NW helper for the NEW cases and says nothing about the existing ones, and AC8 (spec:196-198) constrains counts rather than forbidding fixture-helper edits, so nothing in the spec forces the safe choice. Naming the location in S2 changes what gets built.

### C15 - S5 deletes the only human-readable metric label in the breakdown, and the i18n bullet asserting no user-visible string changes is false

**Severity:** major - **lens:** newbreakage - **spec section:** S5 (spec:25-26), production-readiness i18n (spec:163-164), files-touched (spec:130-138)

**Evidence.** Analyze-SwydoReport.ps1:397 is `$vals[[string]$m.name]=$cell` and :401 is `metricNames=@($mets|ForEach-Object{$_.name})`. S5 repoints BOTH onto the resolved key, so after the change the breakdown carries no display name anywhere - the key IS the label. skill/report-template.md:3 and its Structure section have the model narrate breakdown cells as client-facing prose from `breakdowns[].rows[].values[]`, so on exactly the colliding widgets this unit targets, the published sentence gets a label like `Conversions [google-adwords:all_conversions]`. The spec's own inventory concedes this at spec:93 ("second entry becomes the uniquified key") but files it as a fix, while spec:163-164 claims "i18n - N/A. No user-visible string is added; the uniquified key is already the extraction's own vocabulary". It is the extraction's vocabulary; it is not the client's. skill/report-template.md is absent from the files-touched table at spec:130-138, and F4 (spec:231-235) resolved against touching the template.

**Proposed fix.** Amend S5 to keep identity and label separate: key `$vals` by the resolved key, and carry the display name inside the cell (`$cell.metric=[string]$m.name`) plus a parallel `metricLabels=@($mets|ForEach-Object{$_.name})` beside `metricNames` at :401. Correct the i18n bullet at spec:163-164 to say a machine key replaces a display label in the values map and that the display name is preserved in the cell. Add skill/report-template.md to files-touched with the one line telling the model to label breakdown cells from `values[].metric`, never from the values key. Add an AC that the duplicate-name fixture's published label is the display name, not the bracketed key.

**Skeptic.** Verified against source. `Get-Breakdown` (Analyze-SwydoReport.ps1:388-403) emits per-cell only `display`, `type`, `hasComparison`, `displayPrevious`, `delta` (:394, :396) - no metric name and no metric id. The only two label carriers are the `$vals` key at :397 (`$vals[[string]$m.name]`) and `metricNames` at :401 (`@($mets|ForEach-Object{$_.name})`). S5 (spec:25-26) repoints BOTH onto the resolved key, so after the change the breakdown carries no display name anywhere in the facts document. The consequence is reachable on exactly the fixtures this unit targets, and it is worse than the finding states for the empty-name case. Uniq-Key (Get-SwydoReport.ps1:359) falls back to the metric id when the name is empty, so the spec's own second fixture (spec:83-84, `google-adwords:impressions` with an empty display name, AC2) produces a breakdown values key and a `metricNames` entry of the bare string `google-adwords:impressions`. skill/report-template.md:3 points the model at `breakdowns[].rows[].values[]` and template line 39-40 instructs a prose call-out of a key breakdown row, so that string is the only thing available to label the cell in client prose - while template line 78 reads "Keep the client copy free of internal ids". Nothing in the closer catches this: Test-ReportNumbers.ps1:221-231 iterates `$row.values.PSObject.Properties.Name` purely to index cell display strings and never inspects the key, so a bracketed-id or raw-id label publishes unblocked. The spec's readiness bullet at spec:163-164 ("No user-visible string is added; the uniquified key is already the extraction's own vocabulary") is therefore false as written: the string is new to the FACTS document, which is the surface the model narrates from, and skill/report-template.md is absent from the files-touched table at spec:130-138. spec:93 and AC3 state the mechanical after-state but neither addresses the label consequence, so this is not a restatement. One caveat the builder must carry into the amendment: the proposed fix as written (unconditional `$cell.metric` on every cell plus a parallel `metricLabels`) would add bytes to every breakdown cell of every report and break AC5 (spec:190-191) and the Rollout claim that the canonicalVersion stamp is "the only new bytes" (spec:123). The label carrier must be emitted only where the resolved key differs from `$m.name`, or the spec must consciously widen AC5.

### C16 - The byte-identity precondition says "unique", but Uniq-Key's uniqueness test is case-insensitive, so a case-differing pair flips while satisfying AC5 as written

**Severity:** minor - **lens:** newbreakage - **spec section:** AC5 (spec:190-191), Rollout (spec:119-124)

**Evidence.** Both AC5 and the Rollout paragraph scope the byte-identity claim to reports where "every metric display name in a report is non-empty and unique within its widget". Get-SwydoReport.ps1:360 tests `$map.Contains($base)` against an [ordered] OrderedDictionary, which is case-insensitive (probed: `$m['Conversions']=1; $m.Contains('conversions')` returns True). A widget carrying 'Clicks' and 'clicks' has display names that are unique as strings but collide for Uniq-Key, so metric 2's key becomes `clicks [<id>]` and its facts bytes change. Such a report satisfies AC5's literal precondition and still flips, which means the gate would be asserted on a false premise.

**Proposed fix.** Reword AC5 and the Rollout paragraph to "non-empty and unique CASE-INSENSITIVELY within its widget", and cite Get-SwydoReport.ps1:360 as the reason. Add the case-differing pair to the S8 regression cases so the flip is pinned as expected behaviour rather than discovered in a client report.

**Skeptic.** Verified by probe under PowerShell 5.1: `$m=[ordered]@{}; $m['Conversions']=1; $m.Contains('conversions')` returns True, and property lookup `([pscustomobject]$m).'CONVERSIONS'` returns 1. So Get-SwydoReport.ps1:360 (`if(-not $map.Contains($base))`) collides on case-differing names, and Analyze-SwydoReport.ps1:192-193 (`$row.metrics.$name`) resolves case-insensitively too. Trace for a widget carrying 'Clicks' and 'clicks': the extractor writes keys 'Clicks' and 'clicks [<id2>]' at :525. Today metric 2's lookup `$tr.metrics.'clicks'` hits 'Clicks' and publishes metric 1's value - the same defect class. After the repair metric 2 resolves 'clicks [<id2>]' and its facts bytes change. That report satisfies AC5's literal precondition at spec:190-191 and the identical wording in the Rollout paragraph at spec:120-122 ("non-empty and unique within its widget"), both of which read as string uniqueness, so the byte-identity claim the spec advertises as provable-rather-than-measured has an unstated exception. The gap is not covered by existing tests: Test-Extractor.ps1:33-39 exercises Uniq-Key's dup-name, dup-name-plus-id, null-name and null-both branches, and :65-72 covers the blended same-case 'Clicks' collision, but no case-differing pair exists anywhere in the suite. The correction is small and concrete - reword both spec sites to "unique case-insensitively", cite :360, and add the pair to S8 - but it changes what the AC asserts and adds a regression case, so it changes the build.

### C17 - AC8's assertion-count arithmetic is wrong: 1064 is the eight-suite total, not the other six

**Severity:** minor - **lens:** newbreakage - **spec section:** AC8 (spec:196-198), S9 (spec:36-38)

**Evidence.** AC8 requires "the other six counts unchanged from 1064 in total". I ran all eight suites on this worktree: Test-Analyze 467, Test-Extractor 231, Test-Archive 94, Test-Closer 129, Test-Ledger 47, Test-Sync 4, Test-TrendAnalyze 68, Test-TrendFacts 24 - 1064 across all EIGHT. The other six sum to 366. As written AC8 is unverifiable, since the six suites it names can never total 1064. S9's related line ("invisible to 1064 green assertions") is also loose: the suites that could have caught this class are Test-Analyze and Test-Extractor, 698 assertions.

**Proposed fix.** Rewrite AC8 as: "the other six suites hold at 366 in total (Archive 94, Closer 129, Ledger 47, Sync 4, TrendAnalyze 68, TrendFacts 24), and the eight-suite total rises above 1064 only through Test-Analyze (from 467) and Test-Extractor (from 231)". Reword S9's blind-spot line to 698. Note the suites print two different shapes ('Test-<Name>: N passed' vs 'RESULT: N passed'), so the AC must assert exit codes and per-suite counts, not parse a uniform line.

**Skeptic.** Reproduced by running all eight suites on this worktree: Test-Analyze 467, Test-Extractor 231, Test-Archive 94, Test-Closer 129, Test-Ledger 47, Test-Sync 4, Test-TrendAnalyze 68, Test-TrendFacts 24. Sum = 1064 across all eight; Analyze + Extractor = 698; the other six = 366. AC8 (spec:196-198) requires "the other six counts unchanged from 1064 in total", and those six can never total 1064, so the AC as written is unverifiable at the DoD boundary and the builder needs the corrected figure to assert anything. The suite-output note in the proposed fix is also right and is backed by the observed run: three suites print `Test-<Name>: N passed` (Archive, Closer, Sync) and five print `RESULT: N passed` (Analyze, Extractor, Ledger, TrendAnalyze, TrendFacts), matching the AGENTS.md command-catalog warning to assert exit codes rather than parse the prose. One part of the finding does not survive: the complaint about S9's "invisible to 1064 green assertions" (spec:36-38). All 1064 assertions were green and none caught the defect, so that sentence is literally true; narrowing it to 698 is a stylistic tightening, not a correction. The AC8 arithmetic alone is what confirms.

### C18 - AC5/Rollout byte-identity precondition is false: names unique only case-sensitively still flip

**Severity:** blocker - **lens:** byteidentity - **spec section:** section 4 Rollout (spec:119-124) + AC5 (spec:190-191), also section 4 Data model (spec:60-62)

**Evidence.** The spec states the safe class as "every widget whose metric display names are non-empty and unique within that widget" (spec:120-122, restated in AC5 spec:190-191) and enumerates the divergence classes as "two metrics with the same display name, or one with an empty display name" (spec:61-62). Both are wrong because the collision test is case-insensitive. Get-SwydoReport.ps1:360 tests `if(-not $map.Contains($base))` against `$mmap=[ordered]@{}` built at Get-SwydoReport.ps1:524, and `[ordered]@{}` is a case-insensitive OrderedDictionary. Counterexample, run under the real PowerShell 5.1 (5.1.22621.4249) against HEAD's Uniq-Key with metrics [name 'Conversions' id google-adwords:conversions, name 'conversions' id google-adwords:all_conversions] — both non-empty, distinct as strings, so the spec's precondition holds: key[0]='Conversions', key[1]='conversions [google-adwords:all_conversions]'. So cellKey != $m.name for metric 2. The same probe shows today's analyzer path: `$row.metrics.$('conversions')` returns 100, i.e. metric 1's cell (Analyze-SwydoReport.ps1:738 and :192-193 are case-insensitive property lookups), and the breakdown map at Analyze-SwydoReport.ps1:397 `$vals[[string]$m.name]` collapses to ONE key (probe: `vals count=1 keys=conversions`). After S3-S5 that widget emits two `values` keys instead of one and a changed `metricNames` entry at Analyze-SwydoReport.ps1:401 — a facts byte delta well beyond the canonicalVersion value, on a report the spec declares byte-identical. Not proven live in a client report, but it is free text per widget in Swydo and it is exactly why Uniq-Key's Contains is case-insensitive in the first place: a rows[].metrics JSON object carrying two case-differing keys cannot round-trip into a PSObject.

**Proposed fix.** Restate the precondition everywhere it appears (spec:120-122, AC5 spec:190-191) as "non-empty and pairwise distinct under the case-insensitive comparer `[ordered]@{}` uses" — not "unique". Extend the section 4 Data model enumeration (spec:61-62) to three divergence classes: exact duplicate name, case-only (comparer-equal) duplicate name, empty display name. Add a case-only-duplicate fixture to S8 (spec:34-35) alongside the two existing ones, and add an AC asserting that widget emits two distinct `values` keys and two distinct `metricNames` entries. Add a caution that Uniq-Key must NOT be made case-sensitive as part of this repair: its case-insensitive Contains is what keeps the emitted row map free of keys that would collide on ConvertFrom-Json.

**Skeptic.** Reproduced under the real Windows PowerShell 5.1 (5.1.22621.4249) by dot-sourcing the shipped Get-SwydoReport.ps1 -DefineOnly. With metrics [name 'Conversions' id google-adwords:conversions] and [name 'conversions' id google-adwords:all_conversions] - both non-empty and distinct as strings, so the spec's stated precondition holds - Uniq-Key returned k1='Conversions' and k2='conversions [google-adwords:all_conversions]'. Cause verified at Get-SwydoReport.ps1:360 (`$map.Contains($base)`) against the `[ordered]@{}` built at :524; probe confirms `[ordered]@{}`.Contains and `@{}`.ContainsKey are case-insensitive. Round-tripping that row map through ConvertTo-Json/ConvertFrom-Json and doing the analyzer's lookup (`$o.metrics.$name` with $name='conversions', the shape at Analyze-SwydoReport.ps1:738 and :192-193) returned current=100 - metric 1's cell - while the k2 lookup returned 250. The breakdown probe reproduced the collapse too: writing 'Conversions' then 'conversions' into an `[ordered]@{}` leaves count=1, matching the site at :397. So a widget in the spec's declared safe class does flip after S3-S5, and its facts gain a second `values` key and a changed `metricNames` entry at :401 - a byte delta beyond the canonicalVersion stamp. The spec text is therefore false where it matters: spec:120-122 and AC5 spec:190-191 both say 'unique', which is not the comparer's notion of distinct. (Section 4 Data model at spec:61-62 is only imprecise - 'the same display name' can be read comparer-wise - but the Rollout and AC5 wording cannot.) The fix changes the build: it rewords the safe class, adds a third divergence class, adds an S8 fixture, and its caution against making Uniq-Key case-sensitive is correct - a case-sensitive Contains would let the extractor write two keys that collide when ConvertFrom-Json builds the PSObject.

### C19 - AC5 has no verification method and, as written, demands the full-facts golden this repo already rejected

**Severity:** major - **lens:** byteidentity - **spec section:** AC5 (spec:190-191); section 4 Rollout (spec:119-124)

**Evidence.** AC5 says "the facts document is byte-identical to the pre-change output except for the `canonicalVersion` value" but names no mechanism, and the Rollout paragraph claims the identity is "provable rather than measured" while pointing only at S7's parity gate (spec:123-124) — which asserts key-sequence parity, not facts bytes. There is no golden-file infrastructure to satisfy AC5 literally: Test-Analyze.ps1:329-338 (`RunAnalyze`) writes each run to a fresh GUID temp dir and returns the text, and the ratified house style for exactly this claim is targeted substring pins — Test-Analyze.ps1:642 `$t2.text -match '"current":9999000000,'` and :643 `$t2.text -match [regex]::Escape('"displayCurrent":"$9,999.00"')`. The precedent is explicit: memory/analysis/builds/2026-07-13-ANLZ-aHeadlineRank/spec/2026-07-13-spec-aHeadlineRank-1.md:11 rules that "U9-T2's full-file golden is not sustainable — replace it", because a pre-change golden "cannot be regenerated from the shipped tree and breaks on EVERY future additive unit". AC5 re-introduces that rejected shape.

**Proposed fix.** Rewrite AC5 as a house-style byte guard on a clean fixture: on a widget whose metric display names are comparer-distinct and non-empty, pin the emitted facts text with `-match` assertions on the breakdown `values` key set, the `metricNames` array, one `"current":<n>,` substring and one `displayCurrent` string, plus `meta.canonicalVersion -eq 3` and a negative assertion that no disclosure key appears anywhere in `$r.text`. State in section 4 Rollout that the identity is proven by (a) those pins and (b) the S7 parity gate, and drop the claim that a pre-change output is diffed.

**Skeptic.** Every cited fact checks out. AC5 (spec:190-191) states byte-identity against 'the pre-change output' and names no check; the Rollout (spec:123-124) points only at S7's parity gate, which compares the analyzer's derived key sequence to the extractor's - it never inspects facts bytes, so it cannot substantiate AC5. There is no golden infrastructure: Test-Analyze.ps1:329-338 (RunAnalyze) writes each run into a fresh GUID temp dir and returns the text and parsed facts, with no baseline stored anywhere. The house substitute exists and is exactly the shape proposed: Test-Analyze.ps1:639 (`$t2.text -notmatch 'supersededWidgetId'`), :641 (`canonicalVersion -eq 2`), :642 (`'"current":9999000000,'`) and :643 (the escaped displayCurrent string), all under the comment 'U9-T2 no-flip byte guard'. The precedent is ratified, not invented: memory/analysis/builds/2026-07-13-ANLZ-aHeadlineRank/spec/2026-07-13-spec-aHeadlineRank-1.md:11 is a must-fix amendment ruling the full-file golden unsustainable because it 'cannot be regenerated from the shipped tree and breaks on EVERY future additive unit', and :216 shows the original golden wording it replaced - which is nearly verbatim what AC5 now says. So the spec re-introduces a shape this repo already rejected, and leaves its central rollout claim unverified. The fix adds a concrete fixture and rewrites AC5, so it changes the build.

### C20 - The disclosure's "old value" is not derivable under S3-S5, and cannot exist at all for the empty-name case

**Severity:** major - **lens:** byteidentity - **spec section:** S6 (spec:27-30) vs S3/S4/S5 (spec:21-26); inventory row spec:89

**Evidence.** S6 requires each disclosure entry to name "the metric id, the old value and the new value". S3-S5 repoint every lookup onto the resolved key, which deletes the only thing that could produce an old value — nothing in the post-change analyzer evaluates the pre-repair `$m.name` lookup, so there is no old number to name. Worse, for the spec's own dominant case the old value does not exist: at Analyze-SwydoReport.ps1:738-739 an empty display name makes `$tr.metrics.$($m.name)` return $null and the loop `continue`s, so the metric had NO headline cell — which is exactly what the spec's inventory records at spec:89 ("`google-adwords:impressions` absent entirely" -> "present with `current=900`"). A three-field {metricId, oldValue, newValue} entry cannot represent "was absent", and AC2 (spec:184-185) is precisely that case.

**Proposed fix.** State in S6 that the analyzer evaluates BOTH lookups at the headline site — the legacy `$m.name` lookup and the resolved-key lookup — and emits a disclosure entry only when they disagree; this is also what makes AC5's no-flip claim mechanically checkable rather than asserted. Extend the entry schema to four fields: `metricId`, `oldValue` (JSON null when the legacy lookup missed), `newValue`, and an explicit `wasAbsent` boolean, and say which of the two AC1/AC2 fixtures exercises each shape.

**Skeptic.** The absent-cell half is verified in source and by probe. Analyze-SwydoReport.ps1:738-739 is `$cell = $tr.metrics.$($m.name)` followed by `if(-not $cell){ continue }`; my PS 5.1 probe confirms that a dynamic property lookup with an empty name returns $null, so a metric with an empty display name has no pre-repair headline cell at all. The spec records the same thing itself at spec:89 ('google-adwords:impressions absent entirely' -> 'present with current=900'), and AC2 (spec:184-185) makes that case a first-class acceptance criterion. S6 as narrowed in rev-2 (spec:29-30, rev log spec:241) demands an entry naming 'the metric id, the old value and the new value, and nothing else' - a three-field shape with no representation for 'there was no old value', and no stated convention for whether null means absent or means a non-numeric cell. The derivability half also holds: S3-S5 (spec:21-26) repoint every lookup onto the resolved key and nothing in the spec tells the builder to retain or re-evaluate the legacy `$m.name` lookup, which is the only source of an old value; the spec also never says whether a 'flipped cell' means key-differs or value-differs, and those are not the same set. The fix is actionable against spec text and changes what the code does at :738, so it is more than a clarification.

### C21 - S7/AC4 parity gate is green-by-absence: no fixture matrix, and the replay's comparer is unpinned

**Severity:** major - **lens:** byteidentity - **spec section:** S7 (spec:31-33) and AC4 (spec:188-189)

**Evidence.** S7/AC4 require only that "the analyzer's derived key sequence equals the extractor's `rows[].metrics` key sequence for the same metric list", with no stated fixture set, and section 4 Rollout leans on this gate as the thing that keeps the byte-identity claim "true rather than asserted" (spec:123-124). The gate cannot see the class the claim depends on. The extractor's state is an `[ordered]@{}` (Get-SwydoReport.ps1:524) with a case-insensitive comparer, and Uniq-Key branches on `$map.Contains($base)` (Get-SwydoReport.ps1:360). If the S2 replay accumulates keys in a `[System.Collections.Generic.HashSet[string]]` (ordinal by default — the exact type already used at Analyze-SwydoReport.ps1:813) the case-only-duplicate case derives 'conversions' where the extractor wrote 'conversions [google-adwords:all_conversions]', and every existing fixture still passes because Test-Extractor.ps1:33-39's Uniq-Key cases are all same-case. The AGENTS.md §7 rule against green-by-absence applies directly.

**Proposed fix.** In AC4, enumerate the required fixture matrix rather than "the same metric list": distinct names; exact-duplicate names; case-only-differing names; duplicate name AND duplicate id (the `#idx` tier); empty name with a non-empty id; empty name and empty id (the `col<idx>` tier). In S7, pin the replay's accumulator explicitly as the same `[ordered]@{}` Uniq-Key is called with in Get-SwydoReport.ps1:524 (or a plain `@{}`), and forbid an ordinal comparer, so the two derivations cannot diverge on case.

**Skeptic.** All four factual legs verified. S7 (spec:31-33) and AC4 (spec:188-189) require only equality 'for the same metric list' and name no fixtures, while the Rollout (spec:123-124) elevates that gate to the thing making the byte-identity claim 'true rather than asserted'. The comparer trap is real and measured: my probe shows `[System.Collections.Generic.HashSet[string]]::new()` is ordinal (Contains('conversions') on 'Conversions' => False) while `[ordered]@{}` and `@{}` are case-insensitive (=> True), and the extractor's map at Get-SwydoReport.ps1:524 is the case-insensitive one that Uniq-Key's `$map.Contains($base)` at :360 branches on. The ordinal type is not hypothetical in this codebase - Analyze-SwydoReport.ps1:813 already builds `[System.Collections.Generic.HashSet[string]]::new()` in the same file the S2 replay lands in. Since Uniq-Key takes the map as a parameter and never constructs it, the builder's choice decides the outcome, and a HashSet-backed replay derives 'conversions' where the extractor wrote 'conversions [google-adwords:all_conversions]'. Existing coverage cannot catch it: Test-Extractor.ps1:33-39 exercises 'Clicks' three times same-case plus two null-name cases, so the gate stays green while the invariant is broken - exactly the green-by-absence class AGENTS.md §7 names. Shares a root cause with finding 1 but is a distinct amendment: it changes AC4's fixture matrix and pins the replay's accumulator type in S7.

### C22 - The canonicalVersion 2->3 bump breaks two existing assertions the spec does not list, and AC8 says the suites are additive

**Severity:** major - **lens:** byteidentity - **spec section:** S6 (spec:27-30), section 4 Files touched (spec:136), AC8 (spec:196-198)

**Evidence.** S6 bumps the literal at Analyze-SwydoReport.ps1:996. Two shipped assertions gate on the old value: Test-Analyze.ps1:373 `A ($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1)` and Test-Analyze.ps1:641 `A ($t2.facts.meta.canonicalVersion -eq 2)`. Both go red the moment the literal moves. The Files-touched row for Test-Analyze.ps1 (spec:136) lists only "the `NW` helper and the S8 regression cases", and AC8 (spec:196-198) frames the outcome as "the eight suites are additive on `Test-Analyze.ps1` and `Test-Extractor.ps1` only" — additive-only is not achievable here. U9 hit the same wall and recorded it as "two documented in-place assertion updates" (memory/analysis/builds/2026-07-13-ANLZ-aHeadlineRank/spec/2026-07-13-spec-aHeadlineRank-1.md:168).

**Proposed fix.** Amend the Files-touched row (spec:136) to read "the `NW` helper, the S8 regression cases, and two documented in-place assertion updates (Test-Analyze.ps1:373 and :641, `canonicalVersion` 2 -> 3)". Amend AC8 to "additive in count on Test-Analyze.ps1 and Test-Extractor.ps1, plus exactly two in-place assertion updates, with the other six suite counts unchanged", so a builder does not read "additive" as "touch no existing assertion".

**Skeptic.** Verified at source. Test-Analyze.ps1:373 `A ($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1)` and Test-Analyze.ps1:641 `A ($t2.facts.meta.canonicalVersion -eq 2)` both hard-pin the literal 2. I read the surrounding context (Test-Analyze.ps1:360-381 and :625-643) and both assertions are top-level and unconditional -- no skip guard, no conditional block -- so S6's bump of Analyze-SwydoReport.ps1:996 (`canonicalVersion=2`, confirmed the sole production occurrence by grep) reds both. The spec never names them anywhere: not in S6 (spec:27-30), not in the Files-touched row for Test-Analyze.ps1 (spec:136), not in AC8 (spec:196-198). The precedent citation checks out verbatim: the aHeadlineRank spec's blast-radius section lists "Test-Analyze.ps1 - new cases (test plan below) + two documented in-place assertion updates (e2e11 -> new-rank pin, e2e14 -> `canonicalVersion 2`)", so the last unit to move this literal did name it explicitly and this one does not. I looked for refutations and could not sustain them. The Files-touched heading is labelled "(estimate)", which softens that half, but AC8 is an acceptance criterion and therefore binding, and "additive on Test-Analyze.ps1 and Test-Extractor.ps1 only" is genuinely readable as "touch no existing assertion" -- under that reading S6 and AC8 cannot both be satisfied. Also confirmed no other file pins the literal (grep over the eight suites, skill/scripts/*.ps1, skill/*.md and the manifest returns only these three sites), so the fix is exactly the two sites named. One correction to the finding's framing: severity is nearer major than blocker, since the gates catch it loudly rather than shipping a wrong number. Duplicate of Finding 4.

### C23 - S6's canonicalVersion bump reds two existing Test-Analyze assertions the spec never names

**Severity:** blocker - **lens:** contract - **spec section:** S6 / §4 Files touched / AC8

**Evidence.** Test-Analyze.ps1:373 `A ($r9.facts.meta.canonicalVersion -eq 2 -and $r9.facts.meta.factsVersion -eq 1) "e2e14/U9: meta.canonicalVersion=2 (D6 global bump), factsVersion=1"` and Test-Analyze.ps1:641 `A ($t2.facts.meta.canonicalVersion -eq 2) "U9-T2: canonicalVersion=2 even on non-flip (D6 global)"` both hard-pin the literal 2. The spec's Files-touched row for Test-Analyze.ps1 says only "the NW helper and the S8 regression cases", and AC8 asserts every leg green plus "additive on Test-Analyze.ps1 and Test-Extractor.ps1 only". An implementer following the table literally lands a red Test-Analyze. The precedent unit that last moved this literal did name it explicitly: memory/analysis/builds/2026-07-13-ANLZ-aHeadlineRank/spec/2026-07-13-spec-aHeadlineRank-1.md:168 lists "two documented in-place assertion updates (e2e11 -> new-rank pin, e2e14 -> canonicalVersion 2)".

**Proposed fix.** Add an S-item (or extend S6): "Update the two in-place canonicalVersion pins at Test-Analyze.ps1:373 and :641 from 2 to 3; these are documented in-place assertion updates, not new cases." Amend the Files-touched row for Test-Analyze.ps1 to read "the NW helper, the S8 regression cases, and two in-place canonicalVersion assertion updates (:373, :641)". Reword AC8 so "additive" describes the assertion COUNT and explicitly permits the two value updates.

**Skeptic.** Same claim as Finding 1, independently verified and correct on every particular. Test-Analyze.ps1:373 and :641 quote verbatim, including their assertion messages ("e2e14/U9: meta.canonicalVersion=2 (D6 global bump), factsVersion=1" and "U9-T2: canonicalVersion=2 even on non-flip (D6 global)"). Both execute unconditionally. The production literal is Analyze-SwydoReport.ps1:996 and is the only one. The precedent citation is accurate: the aHeadlineRank spec's blast radius lists "two documented in-place assertion updates (e2e11 -> new-rank pin, e2e14 -> `canonicalVersion 2`)". This finding's proposed fix is the better of the two duplicates because it adds the work as an S-item rather than only amending the Files-touched estimate table, which matters since that table is explicitly labelled "(estimate)" while the S-list is the scope contract. I sustain the claim but not the severity: "blocker" overstates it. Nothing wrong ships -- the merge bar reds immediately and visibly, so this costs a builder a detour and risks a wrong reflex (reverting the bump instead of updating the pins) rather than corrupting output. Treat as major, and de-duplicate against Finding 1 when synthesizing.

### C24 - S6 does not say where the flip disclosure lives, and the wrong placement makes the superseded (wrong) value closer-traceable

**Severity:** blocker - **lens:** contract - **spec section:** S6 / F4

**Evidence.** S6 says only "disclose each flipped cell in-facts, mirroring the disclosure mechanism ratified for the U9 waiver. A disclosure entry names the metric id, the old value and the new value". It names no key and no container. The closer's index is placement-sensitive: skill/scripts/Test-ReportNumbers.ps1:251-252 pulls every number out of `findings[].statement` and `findings[].evidence.*` into byFid; :257-263 pulls `facts.meta.annotations[].text` into byAnnotation; nothing else under `meta` is indexed, and `headline[].canonical` is never read (Build-FactIndex touches only displayCurrent/displayPrevious/displayDelta/type/hasComparison at :215-217). So if the disclosure lands as a finding or an annotation, the OLD WRONG VALUE becomes a legitimate tracing candidate and the closer will certify a report that publishes the number this unit exists to remove. S6 also contradicts F4 on mechanism: U9's mechanism WAS a report-level finding (GAP_HEADLINE_SOURCE_CHANGED, Analyze-SwydoReport.ps1:839-843) plus the inert `canonical.supersededWidgetId` at :768, while F4 resolves "no report-level finding".

**Proposed fix.** Pin the placement in S6: the disclosure is an additive key inside the headline cell's `canonical` block (mirroring U9's `canonical.supersededWidgetId` at Analyze-SwydoReport.ps1:768) — e.g. `canonical.supersededValue` / `supersededDisplay` — and explicitly NOT a finding and NOT `meta.annotations`. Add an AC: "Build-FactIndex over the duplicate-name fixture's facts yields no candidate whose value equals a superseded cell value", asserted in Test-Analyze.ps1 or Test-Closer.ps1 by dot-sourcing Test-ReportNumbers.ps1 -DefineOnly.

**Skeptic.** Verified end to end and the strongest finding of the five. (a) The closer's index is placement-sensitive exactly as claimed: Test-ReportNumbers.ps1:212-218 reads only displayCurrent/displayPrevious/displayDelta off each headline cell -- `canonical` is never touched -- :221-231 reads breakdown cell display/displayPrevious/delta, :245-255 pushes `findings[].statement` and every `findings[].evidence.*` string into byFid, and :257-264 pushes `facts.meta.annotations[].text` into byAnnotation. No other key under `meta` is indexed. (b) The consumption side makes the failure reachable, not theoretical: Test-ReportNumbers.ps1:370-376 adds byFid candidates to any line echoing `<!-- finding:fid -->` and byAnnotation candidates to any line echoing `<!-- annotation:aid -->`, and skill/report-template.md:62-64 instructs the model to quote every `meta.annotations[]` item's `.text` VERBATIM with its aid anchor on that line. So a disclosure placed in meta.annotations gets copied verbatim into the client draft carrying the old value, and the closer then certifies it. That republishes the exact number this unit exists to remove. (c) The mechanism contradiction is real and worse than the finding states. U9's disclosure was two things: the info finding GAP_HEADLINE_SOURCE_CHANGED at Analyze-SwydoReport.ps1:839-843 and the inert `canonical.supersededWidgetId` at :768. S6 forbids the first ("No report-level finding is emitted, per the F4 resolution"), and the comment at :831-832 says the U9 finding was built as "D11-style rollup: ids + count only, NO metric values echoed (the displaced total is deliberately not re-surfaced)". So U9 deliberately withheld values from both of its disclosure surfaces, while S6 requires an entry that "names the metric id, the old value and the new value". There is no U9 precedent for the shape S6 demands, which means "mirroring the disclosure mechanism ratified for the U9 waiver" resolves to nothing a builder can copy. One amendment to the proposed fix before it is adopted: `canonical` exists only on headline cells (Analyze-SwydoReport.ps1:758-766), and this unit's flip set also includes breakdown `values` cells (:394-397), which have no canonical block, so the amendment must name a container for those too or scope the disclosure to headline flips explicitly.

### C25 - AC6's stated equality is arithmetically impossible; the closer's bag grows per breakdown cell, not per dropped metric

**Severity:** major - **lens:** contract - **spec section:** AC6

**Evidence.** AC6: "the closer's per-platform candidate-bag size is recorded before and after, and the growth equals the number of previously dropped metrics." Two independent failures. (a) For the duplicate-name fixture the number of previously dropped HEADLINE metrics is ZERO — Analyze-SwydoReport.ps1:741 keys the headline by `$m.id`, so `google-adwords:all_conversions` is already present carrying metric 1's value; the spec's own inventory says so ("headline value | :738 | all_conversions publishes current=100"). AC6 therefore predicts zero growth. (b) Growth is not 1-per-metric anyway: Test-ReportNumbers.ps1:215-217 adds up to 3 candidates per headline entry (displayCurrent, displayPrevious, displayDelta) and :226-228 adds up to 3 per breakdown cell (display, displayPrevious, delta) for EVERY shown row. The S5 repair splits one `values` key into two per row, so actual growth is roughly (shown rows) x 1..3.

**Proposed fix.** Restate AC6 against the real mechanism: "For the duplicate-name fixture, Build-FactIndex's per-platform candidate count grows by exactly (number of newly-emitted breakdown cells x their populated display fields) plus (3 x newly-present headline entries), asserted by dot-sourcing Test-ReportNumbers.ps1 -DefineOnly and counting `$idx.byPlatform['google-adwords'].Count` against a pinned expected number." Name the fixture that actually has a previously-dropped metric (the empty-display-name one) for the headline half.

**Skeptic.** Both halves check out against source. (a) Analyze-SwydoReport.ps1:741 sets `$key = $m.id`, so the two same-named metrics land under DIFFERENT headline keys today — `google-adwords:conversions` and `google-adwords:all_conversions` are both present, the second merely carrying metric 1's cell (:738 `$cell = $tr.metrics.$($m.name)`). The spec's own inventory row agrees ("all_conversions publishes current=100"), i.e. zero headline metrics are dropped for the duplicate-name fixture. Since Test-ReportNumbers.ps1:215-217 emits a fixed 3 candidates per headline entry and Add-Candidate (:170-177) only skips unparseable displays, the headline bag count is IDENTICAL before and after — only the values change. So under the headline reading AC6 predicts zero growth against non-zero actual growth. (b) The unit is also wrong: Test-ReportNumbers.ps1:226-228 adds display + displayPrevious + delta per breakdown cell for every shown row, and S5 splits one `values` key (Analyze-SwydoReport.ps1:397) into two per row. The inventory's fixture carries comparisons (WIN/LOSS row: `100 vs 80 (+25.0%)`), so `hasComparison` is true and each new cell contributes 3 candidates x N shown rows — never 1-per-dropped-metric. AC6 as written cannot be satisfied by any implementation, so a builder must either fudge the assertion or discover this mid-build. Actionable and build-changing.

### C26 - §3's non-goal for Analyze-SwydoTrend.ps1 rests on a false premise — it consumes the repaired headline

**Severity:** major - **lens:** contract - **spec section:** §3 Non-goals (last bullet)

**Evidence.** The non-goal reads "Any change to Analyze-SwydoTrend.ps1, ConvertTo-SwydoTrendFacts.ps1 or the closer. None of them reads rows[].metrics by display name." True but irrelevant for the trend analyzer: Analyze-SwydoTrend.ps1:263-299 opens the single-report facts via -PeriodKpiFacts and iterates `foreach($hp in $plat.headline.PSObject.Properties)` (:266), keying on `$cell.id` (:270), gating on `$cell.canonical.scope -eq 'account'` (:278), `$cell.unit`/`$cell.currency` (:293) and reconciling `$cell.current` against the ledger. S3 changes exactly those: which metric ids carry a headline cell and what value each holds. So the RECON_TREND_* output changes for any client whose report has a colliding or empty metric display name. ConvertTo-SwydoTrendFacts.ps1 is correctly out of scope (it reads only `$doc.trendCells`, :38, and gates schemaVersion at :75), and the closer is correctly out of scope (it pins neither version literal — verified by grep).

**Proposed fix.** Rewrite the bullet to scope the claim per-file: keep ConvertTo-SwydoTrendFacts.ps1 and the closer as non-goals with the current rationale; move Analyze-SwydoTrend.ps1 into a new §4 subsection stating that its RECON pass is a downstream consumer of the repaired headline, that its behaviour change is intended (it now reconciles metrics it previously skipped, with correct values), and that the trend facts carry no canonicalVersion echo so a trend-facts consumer cannot distinguish repaired from unrepaired input. Add an AC covering one RECON pass over repaired facts.

**Skeptic.** Verified in source. Analyze-SwydoTrend.ps1:263-266 iterates `$PK.platforms` then `$plat.headline.PSObject.Properties` from the single-report facts supplied via `-PeriodKpiFacts`; :270 keys on `$cell.id`, :278 gates on `$cell.canonical.scope -eq 'account'`, :293 on `$cell.unit`/`$cell.currency`, and :309 reconciles `$kpiRaw=$cell.current` against the ledger. S3 changes exactly those inputs: the empty-display-name metric GAINS a headline cell (new loop iteration, hence a new RECON_TREND_COVERAGE info finding at :279 whenever the ledger carries that metric id, gated only by :272-273), and `all_conversions` current flips 100 to 250, which can flip the :318 mismatch verdict. The bullet's stated rationale ("None of them reads rows[].metrics by display name") is literally true but tests the wrong coupling — the coupling is via the facts document, not the row map. Nothing elsewhere in the spec covers it: §4's byte-identity argument and AC5 are scoped to the report facts only, there is no AC over a RECON pass, and grep confirms `canonicalVersion` exists solely at Analyze-SwydoReport.ps1:996 (plus two Test-Analyze assertions), so trend facts carry no version echo and a trend consumer cannot distinguish repaired from unrepaired input. The finding's per-file split is also accurate: ConvertTo-SwydoTrendFacts.ps1 reads only `$doc.trendCells` (:38) and gates schemaVersion at :75, and the closer pins no version literal — both correctly remain non-goals. Adds a design subsection and an AC; build-changing.

### C27 - The repaired empty-display-name metric surfaces with an EMPTY label in both the report facts and the trend RECON findings

**Severity:** major - **lens:** contract - **spec section:** S4 / AC2

**Evidence.** S4 says the Row-Cur/Row-Cmp helpers "keep their $name parameter and gain nothing; only their arguments change" — so only LOOKUPS move to the cellKey, while every LABEL keeps `$m.name`. For the empty-display-name metric that name is the empty string. Analyze-SwydoReport.ps1:759 writes `metric=$m.name` into the headline cell, so the repaired metric publishes as `"metric": ""`. Downstream, Analyze-SwydoTrend.ps1:274 reads `$pkName=if($null -ne $cell.metric){ [string]$cell.metric } else { Get-MetricPart $mid }` — an empty string passes `$null -ne`, so the fallback never fires and every RECON_TREND_COVERAGE statement for that metric reads `google-adwords '': ...`. skill/report-template.md:34 has the model narrate `- <Metric> was/were <displayCurrent>` off that field. AC2 only requires the metric to "appear in the headline", so this passes AC2 while shipping an unlabelled number.

**Proposed fix.** Add S10: "Where a metric's display name is empty, the LABEL fields (`headline[].metric` at Analyze-SwydoReport.ps1:759, and the breakdown `metricNames` entry) fall back to the resolved cell key. Non-empty names are untouched, so byte-identity per AC5 is preserved." Extend AC2: "...appears in the headline with a non-empty `metric` label equal to its resolved cell key."

**Skeptic.** Verified, and the blast radius is larger than the finding states. S4 explicitly freezes labels ("keep their `$name` parameter and gain nothing; only their arguments change"), while Analyze-SwydoReport.ps1:759 writes `metric=$m.name` into the headline cell — so the repaired empty-name metric publishes `"metric": ""`. Analyze-SwydoTrend.ps1:274 is verbatim as quoted, `$pkName=if($null -ne $cell.metric){ [string]$cell.metric } else { Get-MetricPart $mid }`, and an empty string passes `$null -ne`, so every RECON_TREND_COVERAGE statement built from `$covPre="$prov '$pkName': "` (:275) renders `google-adwords '': ...`. Beyond the finding's own evidence: the breakdown-finding statements interpolate `$($primary.name)`, `$($em.name)` and `$($rm.name)` directly into client-narratable prose (Analyze-SwydoReport.ps1:221, :232, :234, :239, :256, :260), and those findings currently cannot fire for an empty-name metric because the lookup returns null — after the repair they fire with an empty metric name mid-sentence, e.g. "'Search' is 53% of ($900 of $1,700)". report-template.md:75 tells the model to quote finding statements and :21 forces surfacing, so the empty label reaches the client copy. AC2 ("appears in the headline") passes on all of that. The spec never decides the label question; it needs an S-item, and the proposed fallback-to-cell-key is the right shape and preserves byte-identity since non-empty names are untouched.

### C28 - The ratified 'sole exception' records for canonicalVersion are not superseded or re-stamped

**Severity:** minor - **lens:** contract - **spec section:** §4 Files touched (estimate) / S6

**Evidence.** Two records assert this waiver is unique. memory/analysis/DECISIONS.md:8: "ANLZ-aGovernedCanon-3 - ... Sole exception: the reviewed, disclosed U9 flip-set waiver, which bumps `meta.canonicalVersion` 1->2." And .claude/SESSION-KICKOFF.md:167-170 carries the same claim as a front-loaded trap. S6 creates a second exception and moves the literal 2->3, falsifying both. AGENTS.md §6 makes the decision log append-only ("never rewrite a ratified record - supersede with a new id + note"), and the manifest ratchet (SESSION-KICKOFF.md:25-32) requires a delta write-back plus a `last-audit` re-stamp when a doc claim it front-loads is found stale. Neither file appears in the Files-touched table and no S-item covers the superseding record.

**Proposed fix.** Add an S-item: "Append a superseding entry to memory/analysis/DECISIONS.md under this unit's id recording the second disclosed waiver (canonicalVersion 2->3); do not edit the ANLZ-aGovernedCanon-3 line." Add Files-touched rows for `memory/analysis/DECISIONS.md` and `.claude/SESSION-KICKOFF.md` (rewrite the :167-170 trap from "Sole exception" to an enumerated exception list, then re-stamp `last-audit` with the delta line per the ratchet).

**Skeptic.** Both citations are exact and both claims go false on land. memory/analysis/DECISIONS.md:8 reads "Sole exception: the reviewed, disclosed U9 flip-set waiver, which bumps meta.canonicalVersion 1->2", and .claude/SESSION-KICKOFF.md:167-170 front-loads the same as a trap. S6 (spec :27-30) creates a second exception and moves the literal 2->3, so both statements become untrue. Worse, §4 Migration (:109-111) mis-reads the record it leans on: it says ANLZ-aGovernedCanon-3 "ratified [this] as the sole exception ... used once before for the U9 waiver", treating a single named exception as a reusable mechanism. On the record's own text it is not -- a second use needs a new ratification, not an appeal to the first. AGENTS.md §6 forbids rewriting a ratified record and requires a superseding id, and no S-item and no Files-touched row covers that; the existing ANLZ-aCandidTally-1 index line at DECISIONS.md:9 records the extraction rationale only, never the waiver. The manifest side is also live rather than hypothetical: the watch pathspec at .claude/SESSION-KICKOFF.md:6 is `AGENTS.md; skill; tools; scripts; memory-tree; memory-recall; .memory-tree.conf; Test-*.ps1`, and this unit touches skill/scripts/*.ps1 and Test-*.ps1, so the ratchet (:25-32) fires and its "a doc/memory claim found stale" trigger applies to the :168 trap specifically. The Files-touched table (:132-138) lists five files and neither of these. This changes the build: two governance edits the spec does not schedule, one of which is the ratification S6 currently assumes it already has.

