# review-aCandidTally-3 - Tier-2 adversarial review of rev-6

**Run:** wf_26d054aa-792 - 2026-08-05 - node a - 14 agents - 5 finder lenses, 4 batched skeptics,
1 synthesis. Target: ANLZ-aCandidTally-1 rev-6 at base 39def66f, BEFORE any code was written.

Precision 20/(20+16) = 0.56.
The synthesized AMENDMENTS block, B1 through B16, is folded into the spec at rev-7.

## Confirmed

| # | Severity | Lens | Spec section | Finding |
|---|---|---|---|---|
| C1 | blocker | claims | §2 S2 / S8; §4 Migration ('Five assertions ... They are in-place updates, not additions') | S2's cellKey stamping silently retires the shipped omit-rather-than-guess guard and reds two undeclared assertions |
| C2 | major | claims | §2 S9 vs §4 Rollout and §6 AC13 | S9's canonical.keyBasis is unconditional, which falsifies AC13 and the Rollout byte-identity claim |
| C3 | major | claims | §6 (AC1-AC20) vs §2 S9; §5 'observability - S9 is the observability surface' | S9 ships the unit's only observability surface with no acceptance criterion and no gate |
| C4 | major | claims | §4 'Files touched (estimate)' and §7 Gates | The kickoff manifest is a watched-gate dependency this unit must update, and the spec touches neither its stamp nor its now-stale suite counts |
| C5 | minor | claims | §2 S5 vs S2 and AC5/AC11 | S5's stated trigger condition is unreachable once S2 runs |
| C6 | minor | claims | §2 S15; §4 Migration; §6 AC20 | S15 does not say what meta.sourceCanonicalVersion holds when -PeriodKpiFacts is absent |
| C7 | major | keyspace | §2 S1 / §4 Files touched (`skill/scripts/_KeySpace.ps1` \| "new; the two key functions and nothing else") | S1 never forbids a param() block in _KeySpace.ps1 — the repo convention that would add one silently voids every -DefineOnly guard downstream |
| C8 | major | keyspace | §4 Alternatives rejected ("Replay `Uniq-Key` inside the analyzer … Dominated by S1: a drift gate is strictly more machinery than not duplicating") / §7 New gates | S1 removes the drift gate but adds no single-definition assertion, despite the identical prior move already carrying one three lines away |
| C9 | blocker | matrix | S5 + S2; §4 Inventory row `matrix value \| :812`; AC5; §4 Files touched | S5's omission precondition is made unreachable by S2, so the matrix inventory row is wrong and two shipped breakdown pins go red |
| C10 | major | matrix | §4 Inventory rows `matrix rank contest` and `matrix conflict \| :885-894`; AC6; AC7 | The winner flip moves `unit`, `currency`, `type`, `basis` and `aggClass` and can flip a `basis-mismatch` conflict; the flip inventory and AC6/AC7 name none of them |
| C11 | major | matrix | AC4 ("matrix and headline agree cell-for-cell over both collision fixtures") | AC4 has no defined comparison and, on the repo's only collision fixture shape, re-asserts a premise Test-Analyze already disproves |
| C12 | minor | matrix | S6 ("Take the label from the winner, via `Metric-Label`"); §4 Rollout byte-identity claim; AC13 | S6's label move is only safe as an in-place re-assignment, and nothing pins matrix-cell key order |
| C13 | major | breakdown | S2 (§2), §4 Migration, S5 (§2), AC5/AC11 (§6) | S2 flips two shipped Test-Analyze assertions that the §4 Migration inventory does not list, and makes both no-cellKey omit paths dead code in the run path |
| C14 | major | breakdown | S8 (§2), §4 Migration, §4 Files touched, AC10 (§6) | S8 falsifies the `valuesByIdScope` self-describing marker, which the spec never moves and which two shipped assertions pin |
| C15 | major | breakdown | AC18 (§6), §4 Migration (the OD-3 residual paragraph) | AC18 is unsatisfiable: S10's version markers are unconditional document-wide literals, so no document can flip a breakdown cell with them "untouched" |
| C16 | minor | breakdown | S7 (§2), §4 Data model, §5 i18n bullet, AC9 (§6) | S7's "leaves every key count identical" understates it: on a comparer-equal collision first-wins changes the published key's spelling, and the membership test must be `.Contains`, not `.ContainsKey` |
| C17 | blocker | disclosure | S2 (and §3 non-goals, §4 Migration/Files touched) | S2 silently reverses the shipped valuesById omit rule and reds two end-to-end assertions |
| C18 | major | disclosure | AC14, AC19, §7 ("the AC14 closer-arithmetic check") | AC14 and AC19's "no candidate equals a superseded value" cannot be asserted on the fixtures they name |
| C19 | major | disclosure | S9, S14 | S9 and S14 never scope "sharing that display name", and the per-provider reading emits a false disclosure |
| C20 | major | disclosure | S15, AC20, §4 Migration | S15's meta key is unconditional and breaks the shipped "-PeriodKpiFacts omitted => byte-identical" invariant |

## Refuted

| # | Severity | Lens | Finding | Why it was dropped |
|---|---|---|---|---|
| R1 | minor | claims | The Files-touched table and test plan were not updated for rev-6's S14 and S15 | The individual observations are factually accurate but none of them changes the build. The table is explicitly headed "### Files touched (estimate)" (spec:189) and every file S14/S15 need is ALREADY a row in it: `skill/scripts/Analyze-SwydoReport.ps1` (S14's finding emission), `skill/scripts/Analyze-SwydoTrend.ps1` ... |
| R2 | minor | claims | The non-goal attributes the index-to-name-to-values two-step to SKILL.md, which instructs the opposite | The citations check out but the finding is a prose-attribution nit on the rationale for an already-WITHDRAWN item, and it changes nothing that gets built. The two-step comment is indeed in the analyzer, at Analyze-SwydoReport.ps1:412-416 ("metricIds[] and metricNames[] are index-aligned, so index -> name -> values[n... |
| R3 | major | keyspace | AC11 becomes tautological once S1 lands — the only acceptance criterion pinning S2 can no longer fail | The finding conflates the shared helper with the new stamper, and AC11 does not reduce to what it claims. S2 (spec:29-32) is `Resolve-CellKeys($w)`, NEW code that stamps `cellKey` onto every record of `$w.metrics` "that lacks one" by calling the helper; "the extractor's own" sequence is `Get-UniqKeySeq` called direc... |
| R4 | major | keyspace | The §4 inventory omits four by-display-name value reads in Get-BreakdownFindings and files their four client-facing statements under "label-only" | The factual half checks out - Analyze-SwydoReport.ps1:219, :220, :224 and :228 read cells via `$primary.name` for a `$primary` selected by metric ID at :218, and the statements at :221, :232, :234 and :239 do carry Format-Metric/Format-Delta output, not just labels. A full grep of by-name reads (:219 :220 :224 :228 ... |
| R5 | major | keyspace | S4's mechanical verification is a manual grep over 27 read sites and is never promoted to a gate, in a file that already carries four source-regex gates | The cited precedents are real (Test-Extractor.ps1:483, :692-695, :696, :701-705, including the anti-zero-match guard at :693) but they are all cases where a source scan is the ONLY possible check: a first-statement ordering inside Connect-Ws, a -TimeoutSec argument on a call no offline suite makes, and a GraphQL que... |
| R6 | minor | keyspace | Adding a ninth file to skill/scripts/ falsifies two counted claims and the manifest baseline block, none of which §4 lists as touched | Verified the surface facts: skill/scripts/ holds exactly 8 .ps1 files today, AGENTS.md:133 says "the eight tools that do the work", skill/SKILL.md:13 enumerates those eight, .claude/SESSION-KICKOFF.md:105-107 carries the per-suite baseline (Extractor 288, Analyze 619, Closer 129, total 1276), and there is no plugin.... |
| R7 | blocker | matrix | S6's aggClass fold-in, taken literally, deletes the documented `not-summable` reason token with zero gate coverage | The mechanical half is accurate — `$aggClass` is computed once at Analyze-SwydoReport.ps1:841 from `$first.metric` and consumed at :854 (`$out.aggClass`), :857 (`Get-MatrixReason $g $aggClass`) and :875 — and the ladder at :831 vs :832 does turn `not-summable` into `incomplete-rows` when the class argument is `$null... |
| R8 | major | matrix | S9's matrix mirroring cannot reach a reason cell as written, and reusing `no-usable-cell` for an ambiguity is indistinguishable from missing data | Both halves fail on inspection. The mirroring half rests on the premise that "the natural implementation (append after the winner is known) reaches only value cells." That premise is contradicted by the spec's own S9 sentence: "Both describe the INPUT, so they are stable and idempotent across reruns." `keyBasis` and... |
| R9 | minor | matrix | `:887` derives every loser's basis from the WINNER's unit, so `basis-mismatch` is currency-only — the same read-the-wrong-object class, in the function S6 edits, left unnamed | The code claim is accurate but the scope claim is not, and the scope claim is the whole finding. Analyze-SwydoReport.ps1:887 does pass the winner's `$m` (`$m` is reassigned to `$win.metric` at :863) with only `$_.currencyCode` varying per loser, and Get-BasisVersion at :564-573 does hash unit into the digest (measur... |
| R10 | blocker | breakdown | S8 widens valuesById into a key space that is not unique: a blank-named metric silently overwrites a collided metric's entry under the same id | The load-bearing premise — 'Today the collided-name gate at `:422` keeps the two populations apart by accident. S8's collided-OR-blank gate merges them' — is false, measured on this tree with the gate UNCHANGED. Fixture: one widget, metrics ('Clicks', google-adwords:clicks) and ('Clicks', google-adwords:clicks); Get... |
| R11 | major | breakdown | S7 leaves a collided breakdown cell undisclosed and unreasoned - the opposite of S5's own omit-with-a-reason standard - while S12 licenses quoting it | The evidence about the channels is accurate (report-template.md:3 whitelists only values[].display/displayPrevious/delta; SKILL.md:40 says SKIP valuesById/metricIds/valuesByIdScope; Build-FactIndex at Test-ReportNumbers.ps1:221-231 walks only $row.values), but the conclusion does not follow. (1) The S5-vs-S7 asymmet... |
| R12 | minor | breakdown | S8 turns `metricNames[]` into a label array that is no longer a key into `values`, and the §3 rationale for keeping `values` name-keyed misquotes SKILL.md | Both halves check out factually and neither changes the build. Half one restates S8. S8 already says: 'A lone blank-named metric currently has no `values` entry and no `valuesById` entry while `metricNames[]` and `metricIds[]` still advertise it, which breaks the index-aligned addressing contract.' That sentence alr... |
| R13 | major | disclosure | S5's absent-cellKey fallback and the §5 'matrix loses coverage' risk are unreachable once S2 lands | The premise is half right and the prescription is wrong in a way that would delete a true risk disclosure. Correct part: after S2 stamps cellKey on every metric (AC11), S5's literal trigger 'where cellKey is absent' does not fire in the production path. But that is a wording imprecision with no build consequence - S... |
| R14 | major | disclosure | The AC18 residual is disclosed nowhere: a breakdown-only flip gets no marker AND no S9/S14 signal | The central claim - 'the flip is invisible in the entire facts document' - is false against this tree, on two independent counts. (1) The breakdown that flips already names the ambiguous ids itself. Get-Breakdown needs only dimensions plus detail rows (:376, :381-382) - no total row - and its valuesById block at :41... |
| R15 | minor | disclosure | S14 omits the emptiness guard, sort and cap that both shipped roll-ups apply | The precedents are quoted accurately - GAP_NO_ACCOUNT_TOTAL guards at :1086, sorts at :1085 and caps at :1087; GAP_HEADLINE_SOURCE_CHANGED guards at :1114, does Sort-Object -Unique at :1115 and caps at :1117 - but the spec already routes the implementer there. S14 does not merely say 'ids plus a count': it says 'It ... |
| R16 | minor | disclosure | §4 Files touched was not updated when rev-6 added S14 and S15 | The textual observation is accurate but immaterial, and the proposed fix is partly wrong. Anchors verified against this tree: Analyze-SwydoReport.ps1:1109 is the GAP_HEADLINE_SOURCE_CHANGED comment with the emitter running to :1124; Analyze-SwydoTrend.ps1:337 is `$factsDoc=[ordered]@{` with `factsVersion=1` at :339 ... |

## Evidence for the confirmed findings

### C1 - S2's cellKey stamping silently retires the shipped omit-rather-than-guess guard and reds two undeclared assertions

**Severity:** blocker - **lens:** claims - **spec section:** §2 S2 / S8; §4 Migration ('Five assertions ... They are in-place updates, not additions')

**Evidence.** REPRODUCED under PS 5.1 against 39def66. Get-Breakdown's valuesById gate at Analyze-SwydoReport.ps1:424 is `if($m.PSObject.Properties['cellKey'] -and $m.cellKey)`, and its comment at :425-426 states the ratified behaviour: 'No cellKey (schemaVersion 2) and a collided name => no way to disambiguate. OMIT rather than publish a confidently wrong number.' S2 stamps a cellKey onto every metric record that lacks one, so that branch becomes always-true. I built the $p4c fixture exactly as Test-Analyze.ps1:1284-1289 does (metrics WITHOUT cellKey, DupRow row map carrying both 'Clicks' and 'Clicks [facebook-ads:clicks]'), called Get-Breakdown before and after stamping via the shared Get-UniqKeySeq: BEFORE valuesById entries = 0; AFTER valuesById entries = 2 (keys google-adwords:clicks | facebook-ads:clicks). That reds Test-Analyze.ps1:1291 (`Count -eq 0`, 'the metric is OMITTED from valuesById, never guessed') and :1292 ('neither colliding id is addressable'). The spec declares only five in-place test edits (the version pins) and S8 says the ONLY valuesById gate change is collided -> collided-or-blank; §3 does not list retiring the ANLZ-aUniformLattice-8 guard, which landed one commit earlier. It is also an undisclosed, unversioned facts-shape change on every real schemaVersion-2 collided document.

**Proposed fix.** Add an S-item: 'S2 makes a schemaVersion-2 collided pair disambiguable, so the no-cellKey omission branch at Analyze-SwydoReport.ps1:425 is deliberately retired; a v2 collided pair now publishes two valuesById entries, each carrying its own number.' Extend §4 Migration's in-place-test-edit list from five to seven, naming Test-Analyze.ps1:1291 and :1292 and what they become. Add an AC pinning the new behaviour. If instead the guard is meant to survive, say so and specify that Resolve-CellKeys is scoped away from Get-Breakdown's widget records - the spec currently implies the opposite, since Get-Breakdown reads the same $w.metrics objects the headline and matrix passes mutate.

**Skeptic.** I tried to refute this and could not. I rebuilt the p4c fixture exactly as Test-Analyze.ps1:1284-1289 does (two metrics both named 'Clicks', NO cellKey, DupRow row map carrying 'Clicks' and 'Clicks [facebook-ads:clicks]'), dot-sourced Analyze-SwydoReport.ps1 -DefineOnly and Get-SwydoReport.ps1 -DefineOnly, and called Get-Breakdown before and after stamping cellKey from Get-UniqKeySeq. Measured: BEFORE valuesById = 0 entries; Get-UniqKeySeq returns ['Clicks','Clicks [facebook-ads:clicks]']; AFTER valuesById = 2 entries, google-adwords:clicks -> 100 and facebook-ads:clicks -> 250. That is exactly the flip the finding claims, and it reds Test-Analyze.ps1:1291 (Count -eq 0) and :1292 (both ids null). Reachability holds structurally, not just in my harness. The guard is at Analyze-SwydoReport.ps1:424 (`if($m.PSObject.Properties['cellKey'] -and $m.cellKey)`) with the ratified rationale at :425-426. Every analyzer pass iterates the SAME $dataWidgets array built once at :913 - the headline loop at :942, the matrix at :1049, and the breakdown at :1249 calling Get-Breakdown at :1253 - so an S2 mutation of $w.metrics records is global to the run. AC11 requires the headline and matrix to resolve through S2, so scoping S2 away from Get-Breakdown is not available without saying so. The spec does not declare this. Section 3 lists 'the valuesById channel' as landed fact, S8 says the only valuesById gate change is collided -> collided-or-blank, and section 4's Files-touched row for Test-Analyze.ps1 enumerates only the section 6 cases, five version-pin updates and the :649 comment. The behaviour change is also correct-and-desirable (the extractor's row loop at Get-SwydoReport.ps1:729 already built v2 row maps with Uniq-Key, so the replayed key is the extractor's own, not a guess) - which is precisely why it needs to be stated as intent rather than discovered as two red assertions. It is an undisclosed facts-shape change on every real schemaVersion-2 collided document.

### C2 - S9's canonical.keyBasis is unconditional, which falsifies AC13 and the Rollout byte-identity claim

**Severity:** major - **lens:** claims - **spec section:** §2 S9 vs §4 Rollout and §6 AC13

**Evidence.** S9 says 'canonical.keyBasis is display-name or cell-key ... Both describe the INPUT, so they are stable and idempotent across reruns' - no emission condition. The headline canonical block is built unconditionally for every headline cell at Analyze-SwydoReport.ps1:994, so an unconditional key lands on every cell of every report. That directly contradicts §4 Rollout ('Such a report is unchanged except for the two version markers') and AC13 ('a no-collision fixture ... facts are unchanged except for the two version markers'). I checked the rest of the scope: S5, S6, S7, S8, S14 and S15 are all collision-conditional or off-document, so S9 is the ONLY item that breaks AC13. The survey this rev folded carried the missing qualifier - reviews/2026-08-05-review-aCandidTally-2.md §5 says 'cell-key is emitted only when the metric's display name was ambiguous in its widget' - and it was dropped at fold-in.

**Proposed fix.** In S9, state the emission condition explicitly: 'keyBasis and ambiguousWith are emitted ONLY on a metric whose display name is ambiguous or blank within its widget; an unambiguous cell's canonical block is byte-identical to today.' Alternatively, if the keys are to be unconditional, rewrite AC13 and the Rollout paragraph as 'unchanged except the two version markers and the additive canonical.keyBasis key' - but then the byte-identity guard no longer means what §4 Rollout claims it means. Pick one; AC13 as worded is the merge bar.

**Skeptic.** The contradiction is real, though one plank of the evidence is misread. Analyze-SwydoReport.ps1:994 builds the canonical block unconditionally inside every headline cell write, so any key S9 adds there lands on every cell of every report. S9 gives keyBasis a two-value domain ('display-name' or 'cell-key') with no emission condition; under the only reading that makes both values meaningful, an unambiguous cell carries keyBasis='display-name'. AC13 ('a no-collision fixture ... facts are unchanged except for the two version markers') and section 4 Rollout ('unchanged except for the two version markers') then cannot both hold. Under the alternative reading (emit only when ambiguous) the 'display-name' value is dead and S9's own wording is wrong. Either way a normative section must be edited, and the choice changes the facts shape of every report, so it changes the build. I checked the finding's claim that S9 is the only AC13-breaker and it survives: the facts assembly at :1274-1290 emits meta, platforms and findings only and never echoes $w.metrics, so S2's stamping is invisible on a no-collision document; S5-S8 and S14 are collision-conditional; S15 writes only the trend document. What I do NOT accept is the provenance claim. reviews/2026-08-05-review-aCandidTally-2.md:294-295 reads 'canonical.keyBasis, one of display-name or cell-key, where cell-key is emitted only when the metric's display name was ambiguous' - the qualifier attaches to the VALUE cell-key, not to the key keyBasis. The survey was equally unconditional; nothing was 'dropped at fold-in'. The defect predates the fold and the proposed spec edit still stands.

### C3 - S9 ships the unit's only observability surface with no acceptance criterion and no gate

**Severity:** major - **lens:** claims - **spec section:** §6 (AC1-AC20) vs §2 S9; §5 'observability - S9 is the observability surface'

**Evidence.** No acceptance criterion mentions keyBasis or ambiguousWith. AC19 pins S14's roll-up, AC20 pins S15's trend echo, AC1-AC18 cover S3-S8 and the markers; S9 is uncovered. This is the one surface §5 names as the observability answer and the one the spec says must never carry a superseded number, and the repo already has the precedent gate for exactly that property: Test-Analyze.ps1:1192 asserts `(($c6 | ConvertTo-Json -Depth 10) -notmatch '250')` for the matrix cell, which S9 explicitly mirrors onto ('Mirrored onto the matrix cell'). Per AGENTS.md §7 every confirmed finding becomes a gate or a documented check; an unpinned disclosure is the ungated-surface class.

**Proposed fix.** Add: 'AC21. When a report carries an ambiguous display name, each affected headline cell and its matrix mirror carry canonical.keyBasis=cell-key and canonical.ambiguousWith listing the OTHER metric ids sharing the name, a ConvertTo-Json of each affected cell matches no superseded value, and a no-ambiguity report carries neither key on any cell.'

**Skeptic.** I read all twenty criteria. AC1-AC18 cover S2-S8, the markers and the residuals; AC19 pins S14's roll-up; AC20 pins S15's trend echo. Neither keyBasis nor ambiguousWith appears anywhere in section 6. I tried the two available refutations and both fail: AC14 pins Build-FactIndex candidates over the collision fixtures (the 'no superseded number' half) but asserts nothing about the disclosure's presence, shape or absence, and AC19's Build-FactIndex clause is scoped to the S14 roll-up document. This is build-changing rather than a checklist nit because of how this spec wires tests: S13 is literally 'Tests, per section 6 and section 7', and section 7's new gates are 'the section 6 fixture suite, and the AC14 closer-arithmetic check'. An item absent from section 6 therefore gets no fixture. S9 is the item section 5 names as the whole observability answer, and the repo already ships the precedent assertion for the property S9 mirrors onto - Test-Analyze.ps1:1192, `(($c6 | ConvertTo-Json -Depth 10) -notmatch '250')`. Note the coupling: this and Finding 2 are the same under-specified item but demand different edits (an emission condition in section 2 versus a new criterion in section 6). Whichever emission rule the owner picks, the AC has to pin it, including the negative case on a no-ambiguity report.

### C4 - The kickoff manifest is a watched-gate dependency this unit must update, and the spec touches neither its stamp nor its now-stale suite counts

**Severity:** major - **lens:** claims - **spec section:** §4 'Files touched (estimate)' and §7 Gates

**Evidence.** .claude/SESSION-KICKOFF.md:6 sets `watch: AGENTS.md; skill; tools; scripts; memory-tree; memory-recall; .memory-tree.conf; Test-*.ps1`. This unit edits skill/scripts/*.ps1 and four Test-*.ps1 files, so scripts/manifest-check.sh check C5 fails at the push boundary AND check C5s fails at commit time: .githooks/pre-commit (core.hooksPath = C:\projects\swydee\.githooks) runs `manifest-check.sh --staged`, whose only green path is the last-audit re-stamp bundled into the SAME commit as the watched change. Separately, .claude/SESSION-KICKOFF.md:103-107 carries the green-count contract with the exact figures this spec quotes as its baseline ('Extractor 288, Analyze 619, Closer 129 ... 1276 assertions across the 8 suites'); S13's new fixtures move Analyze, Extractor and TrendAnalyze, making that manifest claim stale. The 'Files touched' table omits .claude/SESSION-KICKOFF.md entirely, and §7 lists 'the manifest ratchet' as a leg without naming the re-stamp.

**Proposed fix.** Add a Files-touched row: '`.claude/SESSION-KICKOFF.md` | re-stamp `last-audit` (bundled into each watched commit, per the pre-commit staged leg); update the per-suite green counts and the 1276 total at :103-107 to the post-unit figures'. In §7, add a sentence that the ratchet leg only greens with the re-stamp bundled in the same commit, and that the manifest's per-suite figures are part of the merge bar, not documentation.

**Skeptic.** Every mechanical claim checks out. .claude/SESSION-KICKOFF.md:6 sets watch to 'AGENTS.md; skill; tools; scripts; memory-tree; memory-recall; .memory-tree.conf; Test-*.ps1', and this unit edits skill/scripts/*.ps1 plus four Test-*.ps1 files. `git config --get core.hooksPath` returns C:\projects\swydee\.githooks, and .githooks/pre-commit:27-28 runs `scripts/manifest-check.sh --staged`. The C5s leg at scripts/manifest-check.sh:204-218 is explicitly narrowed - its comment says 'a blocking pre-commit cannot see a future follow-up commit, so the bundle form is the only green path here' - and it fails when the staged blob's audit stamp equals HEAD's. So a commit touching skill/ or Test-*.ps1 without a bundled re-stamp is blocked, not merely flagged at the push boundary. Separately, .claude/SESSION-KICKOFF.md:103-107 carries the per-suite green-count contract with the exact figures section 7 quotes as its baseline (Extractor 288, Analyze 619, Closer 129, total 1276); S13's fixtures move three of them. My strongest refutation attempt was that this is generic per-unit DoD boilerplate no spec need restate - and the repo itself kills it. The immediately preceding program's spec, memory/analysis/builds/2026-08-04-ANLZ-aUniformLattice/spec/2026-08-04-spec-aUniformLattice-2.md:230, carries the Files-touched row '`.claude/SESSION-KICKOFF.md` | re-stamp plus the assertion baseline', and its section 7 at :301-303 spells out that the commit re-stamps the manifest and updates the assertion baseline 'because every file P1 touches is on the manifest's watch list'. The house pattern is one commit old; this spec drops it, and its section 7 names 'the manifest ratchet' as a leg without the re-stamp obligation.

### C5 - S5's stated trigger condition is unreachable once S2 runs

**Severity:** minor - **lens:** claims - **spec section:** §2 S5 vs S2 and AC5/AC11

**Evidence.** S5 says: 'Where cellKey is absent and the display name is collided, emit reason=no-usable-cell rather than a value.' S2 stamps a cellKey onto every metric record lacking one, and AC11 requires 'the headline and matrix resolve cell keys through S2', so inside the analyzer cellKey is never absent - the S5 branch as written is dead. The reachable residual is different: the RESOLVED key addresses no numeric cell on the total row, which is what AC5 already describes ('cannot resolve its own cell'). Note also that Get-MatrixReason's third rung at Analyze-SwydoReport.ps1:830 already returns 'no-usable-cell' for an eligible widget with a total row, so a second emission path must not double-classify.

**Proposed fix.** Reword S5's condition to 'where the key returned by Metric-Key addresses no numeric cell on the total row, the contribution stays rank 0 and the cell falls through to the existing no-usable-cell rung at Analyze-SwydoReport.ps1:830', and drop the 'cellKey is absent' framing so S5 and S2 stop contradicting each other.

**Skeptic.** Minor but real, and I could not construct a reachable case. S2 stamps cellKey onto every metric record lacking one, AC11 requires the matrix to resolve through S2, and Get-MatrixContributions at :791 iterates the same $dataWidgets the stamping pass mutates, so inside the analyzer 'cellKey is absent' is false by construction. Uniq-Key (Get-SwydoReport.ps1:403-409) never returns an empty string - it falls back to the id and then to col<idx> - so a blank stamped key is not an escape hatch either. S5 also contradicts S3, which specifies the opposite fallback (absent cellKey -> display name, i.e. the first holder's cell), so the two cannot both describe the same branch. The finding's residual is accurate too: with the read repointed at :812, a key that addresses no numeric cell leaves $rec.cell null and rank 0, $ranked.Count -eq 0 at :850, and Get-MatrixReason's rung at :830 (`$_.eligible -and $_.hasTotalRow`) already returns 'no-usable-cell'. That is AC5's case, handled by shipped machinery with no new code. I weighed refuting this as wording taste. What tips it is the build delta: a builder implementing S5 verbatim writes per-widget display-name collision detection in the matrix pass plus a guarded second emission path, all of it dead, and section 4's grep-based verification ('a grep for a .name-shaped cell read returns hits only inside Get-Breakdown's values block') would not catch dead code. Reword the condition to the key-addresses-no-cell form and the item costs nothing.

### C6 - S15 does not say what meta.sourceCanonicalVersion holds when -PeriodKpiFacts is absent

**Severity:** minor - **lens:** claims - **spec section:** §2 S15; §4 Migration; §6 AC20

**Evidence.** The trend meta block at Analyze-SwydoTrend.ps1:334-347 is emitted on EVERY trend run, including the ordinary ledger-only run with no -PeriodKpiFacts. S15 and AC20 both only describe the handed case ('the trend facts carry meta.sourceCanonicalVersion equal to the source document's canonicalVersion'). Left unstated, the natural implementation emits the key as $null on every ledger-only run, which changes the trend facts shape for a path the spec never analysed - and the spec simultaneously pins trend factsVersion at 1.

**Proposed fix.** Add one sentence to S15: 'The key is OMITTED, never null, when no -PeriodKpiFacts was supplied - an absent key means "no source document", which is different from "a source document with no marker".' Extend AC20 with the negative case: 'and a trend run with no -PeriodKpiFacts emits no sourceCanonicalVersion key at all.'

**Skeptic.** Verified in source and the gap is build-changing. The `-PeriodKpiFacts` branch is `if($myPeriodKpiFacts){` at Analyze-SwydoTrend.ps1:208 and closes at :327. The meta block is at Analyze-SwydoTrend.ps1:337-349, entirely OUTSIDE that branch, so it is emitted on every trend run including the ordinary ledger-only one. Every key in it (:339-348) is an unconditional inline assignment in a flat `[ordered]@{}` literal, so the natural edit for S15 is `sourceCanonicalVersion=$srcCv` inline — which evaluates to $null whenever no -PeriodKpiFacts was passed. That is not a cosmetic gap: Analyze-SwydoTrend.ps1:206-207 carries a standing in-source contract that "When -PeriodKpiFacts is OMITTED, emit ZERO RECON_* findings and the trend path stays byte-identical", and .claude/SESSION-KICKOFF.md:171-172 makes byte-for-byte stability of a default output path a waiver-gated property. AC20 (spec:272-274) pins trend `factsVersion` at 1, so a null key would change the default ledger-only trend document under an unmoved marker — precisely the thing the constraint forbids. Nothing in the spec covers the absent case: S11 (spec:62-64), S15 (spec:73-76), the Migration paragraph (spec:166-168) and OD-2 (spec:297-301) all speak only to the handed case. The existing suite would not catch it either — Test-TrendAnalyze.ps1:251 asserts only `$anyRecon.Count -eq 0`, never the meta shape. The repo already has the correct discipline written down one file over (Analyze-SwydoReport.ps1:441-442, "OMITTED, never null: an absent key means ... which is different from ..."), which makes the omission in S15 a genuine incompleteness rather than an implied default. The proposed edit — omit rather than null, plus the negative case on AC20 — is the right fix.

### C7 - S1 never forbids a param() block in _KeySpace.ps1 — the repo convention that would add one silently voids every -DefineOnly guard downstream

**Severity:** major - **lens:** keyspace - **spec section:** §2 S1 / §4 Files touched (`skill/scripts/_KeySpace.ps1` | "new; the two key functions and nothing else")

**Evidence.** All eight existing files in skill/scripts/ open with `param(...[switch]$DefineOnly)`, and five of them dot-source a peer with `-DefineOnly`. A dot-sourced script's param() block REBINDS those names in the CALLER's scope — the trap Analyze-SwydoTrend.ps1:29 and ConvertTo-SwydoTrendFacts.ps1:30-31 already work around with `$my*` copies. Reproduced against real PS 5.1 (5.1.22621.4249) with a minimal Analyzer.ps1 + _KeySpace.ps1 pair: dot-sourcing a param-carrying _KeySpace.ps1 set `$DefineOnly=$false` and `$InFile=''` in the analyzer's own scope, so the analyzer fell straight through `if($DefineOnly){ return }` (Analyze-SwydoReport.ps1:898) into its run body and threw `InFile is required`. Same shape voids Get-SwydoReport.ps1:953. That reds Test-Analyze, Test-Extractor, Test-Ledger, Test-TrendAnalyze, Test-TrendFacts and Test-Archive at once, with a failure message that names neither _KeySpace.ps1 nor the dot-source. The spec's phrase "holding nothing else" is the closest it comes to prohibiting this, and it does not read as a prohibition. (Verified separately that the rest of S1's mechanism DOES work: `$PSScriptRoot` inside a dot-sourced script resolves to that script's own directory, so `. "$PSScriptRoot\_KeySpace.ps1"` loads correctly through Test-Extractor.ps1:9 → Get-SwydoReport.ps1 and through Test-Analyze.ps1:7 / Analyze-SwydoTrend.ps1:31 → Analyze-SwydoReport.ps1 without editing any dot-source line, and neither Uniq-Key nor Get-UniqKeySeq closes over script scope.)

**Proposed fix.** In S1, add: "`_KeySpace.ps1` carries NO `param()` block, no `-DefineOnly` switch and no early return — it is a bare function library, dot-sourced as `. \"$PSScriptRoot\\_KeySpace.ps1\"` with no arguments. A `param()` block on a dot-sourced file rebinds its declared names in the CALLER's scope, which would set `$DefineOnly=$false` and `$InFile=''` inside Analyze-SwydoReport.ps1 and Get-SwydoReport.ps1 and drop both through their `-DefineOnly` guards into their run bodies. This is the only file in `skill/scripts/` that deliberately breaks the functions-first `param()` convention, and the reason is recorded here."

**Skeptic.** I reproduced the mechanism myself in real Windows PowerShell 5.1 (powershell.exe, separate from the pwsh 7.6.3 host): a two-line host script dot-sourcing a param-carrying peer printed `after-dotsource DefineOnly=False InFile=''` and then threw `InFile is required` — the caller's own `-DefineOnly` was silently rebound to the peer's default. Applied to this design that lands on Analyze-SwydoReport.ps1:898 (`if($DefineOnly){ return }`) and :900 (`if(-not $InFile){ throw "InFile is required" }`), and on Get-SwydoReport.ps1:953. Both dot-sources must sit ABOVE those guards because `Uniq-Key` is consumed inside the row loop at Get-SwydoReport.ps1:729 and `Get-UniqKeySeq` at :711, so the failure is reachable, not theoretical. The convention pressure is real and stronger than the finding claims: all eight files in skill/scripts/ carry `param(...[switch]$DefineOnly)`, and .claude/SESSION-KICKOFF.md:163-164 — the project layer that outranks AGENTS.md on project facts — instructs "Hardened scripts are reused via `-DefineOnly` dot-sourcing (functions-first pattern)" and prescribes the CALLER-side `$my*` workaround. Following that manifest line would mean adding `$my*` copies to Analyze-SwydoReport.ps1 and Get-SwydoReport.ps1, i.e. behaviorally editing two hardened scripts, which is the opposite of what S1 intends. The spec's "holding nothing else" (spec:26) and "the two key functions and nothing else" (spec:193) do not read as a prohibition on a param block and disclose none of this; the Alternatives-rejected entry at spec:207-208 only rejects sourcing `Get-SwydoReport.ps1 -DefineOnly`, which reinforces rather than blocks the pattern. The blast radius is six suites (Test-Analyze, Test-Extractor, Test-Ledger, Test-TrendAnalyze, Test-TrendFacts, Test-Archive) failing with a message naming neither _KeySpace.ps1 nor the dot-source. The proposed S1 sentence — no param(), no -DefineOnly, no early return, with the rationale recorded — is a concrete edit that changes what gets written.

### C8 - S1 removes the drift gate but adds no single-definition assertion, despite the identical prior move already carrying one three lines away

**Severity:** major - **lens:** keyspace - **spec section:** §4 Alternatives rejected ("Replay `Uniq-Key` inside the analyzer … Dominated by S1: a drift gate is strictly more machinery than not duplicating") / §7 New gates

**Evidence.** Test-Ledger.ps1:186 pins exactly this invariant for the previous MOVED-for-single-definition helper: `Assert ((Get-Command Get-BasisVersion).ScriptBlock.File -like "*Analyze-SwydoReport.ps1")`, with the comment at :181-184 explaining that Test-Ledger is the suite loading both files so the single-definition rule can be asserted mechanically. S1 makes the same move for `Uniq-Key`/`Get-UniqKeySeq` and gates nothing: §7's "New gates" lists only the §6 fixture suite and the AC14 closer-arithmetic check. Nothing then stops a future session from re-adding a local `Uniq-Key` to Analyze-SwydoReport.ps1 — the analyzer's whole key resolution would silently revert to a second copy that can drift from the extractor's row-map keys, which is the defect S1 exists to make structurally impossible. AGENTS.md §7 requires "Lockstep invariants get a guard … a gate, not memory."

**Proposed fix.** Add to §6/§7 a gate mirroring the in-tree precedent, in the suite that will load both scripts after AC12: "AC21. `(Get-Command Uniq-Key).ScriptBlock.File` and `(Get-Command Get-UniqKeySeq).ScriptBlock.File` both end in `_KeySpace.ps1`, asserted in `Test-Extractor.ps1` (the suite that loads the extractor and, per AC12, the analyzer), so a re-added local copy in either script fails loudly." List it under §7 New gates alongside the fixture suite.

**Skeptic.** Verified in tree. Test-Ledger.ps1:185-187 is the exact precedent, with the comment at :181-184 stating the reasoning explicitly ("Test-Ledger is the suite that loads BOTH files, so it is where the single-definition rule can be asserted mechanically"). The spec's §7 lists only the §6 fixture suite and the AC14 closer check, and §4 Alternatives-rejected actively argues the guard away ("a drift gate is strictly more machinery than not duplicating") - which is true of the DUPLICATION but not of the guard against re-duplication. I tried to refute this by finding a behavioral substitute in the spec and could not, and the reason is stronger than the finding states. AC11 ("the stamped sequence equals the extractor's own on the identical metric list") looks like a drift gate but is provably vacuous for this failure under PS 5.1: functions live in one session namespace. Test-Extractor.ps1:9 dot-sources Get-SwydoReport.ps1 -DefineOnly, Test-Analyze.ps1:7 dot-sources the analyzer; after S1 the only definitions in a session are _KeySpace's and, if a local copy were re-added below the analyzer's dot-source, the analyzer's - which shadows globally. Both sides of any equality assertion would then call the SAME surviving definition and agree, while the two production processes diverge. No fixture, in-process or via RunAnalyze's child process (Test-Analyze.ps1:388), can distinguish it. `(Get-Command Uniq-Key).ScriptBlock.File` is the only mechanism that can, which is exactly why the precedent at :186 is written that way. AGENTS.md §7 binds this ("Lockstep invariants get a guard ... a gate, not memory"), and the proposed AC21 is mechanically feasible: after S1 the extractor's dot-source resolves Uniq-Key/Get-UniqKeySeq to _KeySpace.ps1, and AC12 already brings the analyzer into Test-Extractor.ps1. The gate is additive on that suite's count, so it does not violate the green-count contract. Spec is incomplete in a way that changes the build.

### C9 - S5's omission precondition is made unreachable by S2, so the matrix inventory row is wrong and two shipped breakdown pins go red

**Severity:** blocker - **lens:** matrix - **spec section:** S5 + S2; §4 Inventory row `matrix value | :812`; AC5; §4 Files touched

**Evidence.** S5 fires only "where `cellKey` is absent and the display name is collided". S2 stamps `cellKey` onto every metric that lacks one, and §2 calls S2 load-bearing precisely because every e2e fixture is `schemaVersion=2` (Test-Analyze.ps1:405). The spec never says WHERE `Resolve-CellKeys` is invoked, so under the only reading that makes S2 load-bearing (once over the widgets, before the layers run) `Metric-Key` always returns a cellKey and S5's clause is dead: the §4 inventory row `matrix value | :812 | 100 ... | 250, or reason='no-usable-cell'` can only ever be 250. Verified under PS 5.1 against this tree: `Get-UniqKeySeq` over the exact metric list at Test-Analyze.ps1:1284 (`$dupNoKey`, two metrics both displayed `Clicks`, ids `google-adwords:clicks` / `facebook-ads:clicks`) returns `Clicks` and `Clicks [facebook-ads:clicks]` — byte-identical to the row-map keys `DupRow` writes at Test-Analyze.ps1:1264. So after S2 the same-shaped omit branch already shipped in `Get-Breakdown` (Analyze-SwydoReport.ps1:424-427, the rule S5 says it is "matching") also stops firing, and these two shipped assertions invert: Test-Analyze.ps1:1291 `"P4 AC4 v2 + collided name => the metric is OMITTED from valuesById, never guessed"` (0 -> 2 entries) Test-Analyze.ps1:1292 `"P4 AC4 v2 neither colliding id is addressable, so no wrong number is published"` Neither line appears in the §4 Files-touched row for `Test-Analyze.ps1` (which lists only "the §6 cases; five in-place version-pin updates; the stale header comment at :649"), and §4 Rollout claims a no-collision report is "unchanged except for the two version markers" without noting that every COLLIDED schemaVersion-2 report gains `valuesById` entries it did not have.

**Proposed fix.** In S2, state the invocation site and scope of `Resolve-CellKeys` explicitly (which widget list, at which point in the run block, and whether it runs before `Get-MatrixContributions` and `Get-Breakdown`). Then pick one and write it down: (a) S2 runs universally — delete S5's "where cellKey is absent and the display name is collided" clause, change the §4 inventory row for `:812` to `250` with no `or` branch, re-point AC5 at the already-shipped echo-object path (pinned at Test-Analyze.ps1:1170) so it is not a vacuous criterion, and add Test-Analyze.ps1:1290-1293 to the Files-touched row as in-place inversions with their new expected values plus a note that this is a deliberate coverage GAIN; or (b) scope S2 so it does not reach the breakdown/matrix reads, and say so in S2, S5 and S7. Add an AC pinning whichever behaviour is chosen for a schemaVersion-2 collided document.

**Skeptic.** Reproduced the mechanism end to end against this tree. S2 says Resolve-CellKeys "stamps cellKey onto every record of $w.metrics that lacks one" and never says where it is invoked; the spec's own justification for it being load-bearing (every e2e fixture is schemaVersion=2, confirmed at Test-Analyze.ps1:405 where MkDoc hardcodes it) only holds if it runs over the widgets before the layers, mutating the same $w objects the breakdown and matrix later read. Under that reading the omit branch at Analyze-SwydoReport.ps1:422-427 stops firing. The keys line up exactly: Uniq-Key (Get-SwydoReport.ps1:403-408) over the $dupNoKey list at Test-Analyze.ps1:1284 yields 'Clicks' then 'Clicks [facebook-ads:clicks]' (pinned independently at Test-Extractor.ps1:35-36 and :86), byte-identical to the row-map keys DupRow writes at Test-Analyze.ps1:1264. So Test-Analyze.ps1:1291 ("the metric is OMITTED from valuesById, never guessed") goes 0 -> 2 entries and :1292 inverts with it. §4's Files-touched row for Test-Analyze.ps1 enumerates "the §6 cases; five in-place version-pin updates; the stale header comment at :649" and does not include them, and no §4 inventory row covers "collided v2 document gains valuesById entries". By the same token S5's stated precondition ("where cellKey is absent and the display name is collided") is dead, so the inventory row for :812 can only ever read 250. I checked whether that makes AC5 vacuous and it does not - the echo-object path still yields no-usable-cell and is already pinned at Test-Analyze.ps1:1170 - which is exactly why the finding's fix (a) is the right shape. I cannot refute this on "the flip is intended and safe." It probably IS safe, since the stamped key reproduces the extractor's key space rather than guessing. But the spec never states the invocation site, so both readings are open, and two shipped assertions whose names assert "never guessed" and "no wrong number is published" hang on the answer. That is an incompleteness the builder cannot resolve without inventing design.

### C10 - The winner flip moves `unit`, `currency`, `type`, `basis` and `aggClass` and can flip a `basis-mismatch` conflict; the flip inventory and AC6/AC7 name none of them

**Severity:** major - **lens:** matrix - **spec section:** §4 Inventory rows `matrix rank contest` and `matrix conflict | :885-894`; AC6; AC7

**Evidence.** The inventory says the rank contest moves and "`scope` and `method` can flip"; AC6 lists `scope`, `method` and `contributingWidgetIds`; AC7 pins only `same-rank-disagreement`. But Analyze-SwydoReport.ps1:863 reassigns `$m=$win.metric` and `$cc=$win.currencyCode`, and :870-876 then derive `type`, `unit`, `currency` and `basis` (via `Get-CellBasis`) from the winner, while :875 publishes `aggClass`. All of them move with the winner, and `basis.basisVersion` is a SHA over (id, unit, currency). Verified under PS 5.1 with `Reduce-MatrixCell` called directly on two same-rank `google-adwords:cost_micros` contributions, USD at documentIndex 0 and EUR at documentIndex 1: both ranked -> `currency=USD`, `conflict.reason=basis-mismatch` doc-first rank set to 0 -> `currency=EUR`, `conflict` ABSENT, `contributingWidgetIds=[f-b]`, `observedOnWidgetIds=[f-a,f-b]` Dropping the doc-first contribution's rank is exactly what S5/AC6 produce when a rank-1 KPI's own cell turns out to be an echo, so a `basis-mismatch` conflict silently disappearing is inside this unit's flip set and is currently unlisted and unasserted. `observedOnWidgetIds` is confirmed invariant (computed at :842 from the whole group, rank-independent) — that half of the spec's implicit claim holds.

**Proposed fix.** Extend the §4 inventory row for the rank contest to read `scope`, `method`, `unit`, `currency`, `type`, `aggClass` and `basis.basisVersion` all move with the winner, and add one sentence stating `observedOnWidgetIds` is invariant by construction (`:842` is rank-independent) so a reviewer does not have to re-derive it. Extend AC6 to assert `unit`, `currency` and `basis.basisVersion` move together with `scope`/`method`/`contributingWidgetIds`, and add an AC alongside AC7: "When the winner flips across a currency boundary, a `basis-mismatch` conflict both appears and disappears", using the USD/EUR two-KPI shape already shipped at Test-Analyze.ps1:1195-1202.

**Skeptic.** I tried to refute this and could not. Analyze-SwydoReport.ps1:863 reassigns `$m=$win.metric` and `$cc=$win.currencyCode`, and :870-876 then derive `type`, `unit`, `currency` and `basis` from those — so a winner change moves all four inside the value branch, no value/reason line-crossing required. `Get-BasisVersion` at :564-573 hashes `(metricId, unit, currency)`, so `basis.basisVersion` moves with them. Post-S6, `aggClass` at :875 joins the set. The spec's §4 inventory row for the rank contest names only `scope` and `method`; AC6 names `scope`, `method`, `contributingWidgetIds`; AC7 pins only `same-rank-disagreement`. None of the other five keys appears anywhere in the spec. The conflict half holds too. `$offBasis` at :887 compares `Get-BasisVersion $m.id $m.unit $_.currencyCode` against `$out.basis.basisVersion` with `$m` already the WINNER's metric, and `$losers` at :885 is derived from `$ranked` — so dropping the doc-first contribution's rank (exactly what S5/AC6 produce when a rank-1 KPI's own cell turns out to be unresolvable) both flips `currency` and empties the loser pool, deleting a `basis-mismatch` conflict. The shipped USD/EUR shape at Test-Analyze.ps1:1195-1201 is collision-free, so it stays green either way and asserts nothing about this path. What makes this a spec defect rather than a re-derivation: the prior review already found it and the rebase dropped it. reviews/2026-08-05-review-aCandidTally-2.md §1 lists "also `type`, `unit`, `currency` and `basis.basisVersion`" as changeable, and its §3 states plainly that `basis-mismatch` "uses `$m` = the winner's metric record, so a winner change swaps the baseline unit and can flip which losers count as off-basis" and that "`conflict.reason` can change token without any conflict appearing or disappearing" — a third mode the spec captures nowhere. The spec's §4 inventory is presented as the bounded flip set (the repo's own discipline, pinned at Test-Analyze.ps1:1334), so an incomplete flip set is a build-changing gap: without the AC, no fixture with two currencies and a collision gets built. The finding's `observedOnWidgetIds`-is-invariant half also checks out at :842.

### C11 - AC4 has no defined comparison and, on the repo's only collision fixture shape, re-asserts a premise Test-Analyze already disproves

**Severity:** major - **lens:** matrix - **spec section:** AC4 ("matrix and headline agree cell-for-cell over both collision fixtures")

**Evidence.** Two problems. (1) "Cell-for-cell" is undefined: a headline cell and a matrix cell do not share a key set — the matrix carries `scope`, `method`, `aggClass`, `basis`, `contributingWidgetIds`, `observedOnWidgetIds`, `coverageBasis`, `period`; the headline carries `canonical{}`. The only shipped assertion of this claim, Test-Analyze.ps1:1145, compares `displayCurrent` alone, which is weak enough to be satisfied by a one-sided repair of everything else. (2) The repo's only collision fixtures — `$dupMets` at Test-Analyze.ps1:1259 and `$l8Mets` at :1373 — put `google-adwords:clicks` and `facebook-ads:clicks` on one widget whose `providers[0].id` is `google-adwords`. That is a mixed-prefix widget: the headline attributes by `Get-WidgetProvider` (Analyze-SwydoReport.ps1:186, widget-level) so both ids land under `google-adwords`, while the matrix attributes by `Get-MetricProviderId` (:783, metric prefix) so `facebook-ads:clicks` lands under `facebook-ads`. Test-Analyze.ps1:1324-1332 pins that divergence as deliberate and review-reproduced, and :1309-1322 pins the second divergence (the matrix refuses a hidden-section widget, the headline does not). If AC4's fixtures reuse the established collision shape, AC4 is unsatisfiable for reasons that have nothing to do with this repair.

**Proposed fix.** Rewrite AC4 as: "When the matrix and the headline are compared over the (platform, metricId) pairs present in BOTH layers, they agree on `id`, `current`, `previous`, `deltaPct`, `displayCurrent`, `displayPrevious`, `displayDelta`, `type`, `direction`, `unit`, `currency`, `hasComparison` and the resolved label; the matrix's provenance keys and the headline's `canonical{}` are out of scope." Add a sentence stating the two collision fixtures are single-prefix and visible-section by construction, and that the mixed-prefix and hidden-section divergences pinned at Test-Analyze.ps1:1309-1332 remain legitimate and are explicitly excluded — otherwise AC4 reads as reversing them.

**Skeptic.** Both halves verify, and the second is worse than stated. Part 1: the two layers do not share a key set. The headline entry built at :988-999 carries `metric, id, unit, type, direction, currency, current, previous, deltaPct, hasComparison, displayCurrent, displayPrevious, displayDelta, canonical`; the matrix value cell built at :849-896 carries `id, metric, current, previous, deltaPct, displayCurrent, displayPrevious, displayDelta, type, direction, unit, currency, hasComparison, scope, method, aggClass, basis, contributingWidgetIds, observedOnWidgetIds, coverageBasis, period[, conflict]` — different members, different order, and `scope`/`method` live under `canonical` on the headline side. "Cell-for-cell" is not implementable literally. The repo's only precedent for the phrase is Test-Analyze.ps1:1145, which carries the identical wording and compares `displayCurrent` alone. An AC labelled "the mechanical prohibition on a one-sided repair" whose strength is entirely in an unnamed field set, implemented against a one-field precedent, is a real gate gap. Part 2: the repo's only collision fixtures are `$dupMets` at Test-Analyze.ps1:1259 and `$l8Mets` at :1373, both `google-adwords:clicks` + `facebook-ads:clicks` on a widget whose `providers` is built by `DW` (:395-399) as a single entry `google-adwords`. The divergence is sharper than the finding claims: the headline attributes by `Get-WidgetProvider` so both ids land under `google-adwords`, while the matrix attributes by `Get-MetricProviderId` (:783) to `facebook-ads` — and `facebook-ads` is never a discovered platform, so `:1060` (`if(-not $platforms.ContainsKey($prov)){ continue }`) drops the cell entirely. The headline has an entry with NO matrix counterpart at all. Test-Analyze.ps1:1309-1322 and :1324-1332 pin both this attribution divergence and the hidden-section one as deliberate and review-reproduced. AC4 over that shape is unsatisfiable under any total comparison and says nothing about which fixtures it means, while §12 reuse discipline points an implementer straight at `$dupMets`. Scoping AC4 to the intersection and stating the fixtures are single-prefix and visible-section changes the build.

### C12 - S6's label move is only safe as an in-place re-assignment, and nothing pins matrix-cell key order

**Severity:** minor - **lens:** matrix - **spec section:** S6 ("Take the label from the winner, via `Metric-Label`"); §4 Rollout byte-identity claim; AC13

**Evidence.** `$out` is built as an `[ordered]@{ id=...; metric=... }` at Analyze-SwydoReport.ps1:849, so `metric` is the second key of every matrix cell in every report. Re-assigning `$out.metric` after :863 keeps its index (OrderedDictionary preserves position on assignment to an existing key); dropping `metric` from the :849 literal and appending it after the winner is picked moves it to the END of the cell, changing the JSON key order of EVERY matrix cell on EVERY report — a byte-identity break under the §4 Rollout claim that a no-collision report changes only in the two version markers. No shipped assertion guards this: key order is pinned for the platform object (Test-Analyze.ps1:1151), facts top level (:1230), the breakdown block (:1242) and the breakdown row (:1246), but not for a matrix cell. AC13 would catch the value-branch case incidentally; nothing at all covers the reason branch, which is where S9's new keys also land.

**Proposed fix.** Add to S6: "`metric` and `aggClass` are RE-ASSIGNED in place on the existing `[ordered]` entries created at `:849`/`:875`; the `$out` literal at `:849` keeps both keys so cell key order is unchanged." Add an acceptance criterion pinning matrix-cell key order for both branches, in the shape of the existing pins at Test-Analyze.ps1:1242 and :1246 — one for a value cell and one for a reason cell — so S9's additions and any later reshuffle have a gate.

**Skeptic.** Verified, and the exposure is broader than the finding says. `$out` is built as `[ordered]@{ id=...; metric=... }` at Analyze-SwydoReport.ps1:849, so `metric` is the second key of every matrix cell in every report. Re-assigning `$out.metric` after :863 keeps the slot; dropping it from the literal and appending it moves it to last on every value cell AND removes it entirely from every reason cell, since :850-857 never writes it. Neither outcome is gated. Every key-order pin in the suite is at Test-Analyze.ps1:678 (meta.period), :1151 (platform), :1230 and :1231 (facts top level and findings channels), :1242 (breakdown block) and :1246 (breakdown row) — not one covers a matrix cell. The only assertion touching a matrix cell's `metric` is :1365 (`UL-2 the cell's NAME and VALUE come from the same contribution`), which reads the member by name and so passes unchanged when the key merely moves to the end; and it never runs against a reason cell. The totality loop at :1223 checks `aggClass`, `basis` and `period`, not `metric`. The byte-identity claim in §4 Rollout rests entirely on AC13, and the same paragraph explicitly declines a full-facts golden. The repo's precedent for that kind of guard is Test-Analyze.ps1:719-730, which pins substrings of `$t2.text` — a shape that would not catch a cell-level key reorder. So the spec makes a byte-identity claim about every matrix cell in every report and provides no mechanism that can observe a violation of it, in the same unit that both touches the `metric` key at :849 and adds two new S9 keys to the same object with no stated position. A spec sentence mandating in-place re-assignment plus one value-cell and one reason-cell key-order AC closes it; both are build changes.

### C13 - S2 flips two shipped Test-Analyze assertions that the §4 Migration inventory does not list, and makes both no-cellKey omit paths dead code in the run path

**Severity:** major - **lens:** breakdown - **spec section:** S2 (§2), §4 Migration, S5 (§2), AC5/AC11 (§6)

**Evidence.** `Test-Analyze.ps1:1284-1292` builds `$dupNoKey` with NO cellKey and a collided display name, runs it through `RunAnalyze` (which writes a real `meta.schemaVersion=2` doc at `Test-Analyze.ps1:405` and invokes the actual analyzer at `:415`), and pins `valuesById` empty. S2 stamps every schemaVersion-2 metric, and the fixture's own row map (`DupRow`, `Test-Analyze.ps1:1262-1268`) already carries exactly the keys Get-UniqKeySeq derives. Measured (Get-Breakdown, -DefineOnly, cellKeys stamped as S2 would): Get-UniqKeySeq -> Clicks , Clicks [facebook-ads:clicks] valuesById keys AFTER stamping: [google-adwords:clicks | facebook-ads:clicks] Test-Analyze.ps1:1291 (.Count -eq 0) => False Test-Analyze.ps1:1292 (both id lookups are null) => False §4 Migration enumerates exactly five in-place test updates (`:459`, `:460`, `:728`, `:1300`, `:1301`) and names none of these. Two shipped safety pins - "never guessed" and "no wrong number is published" - are deleted without being budgeted. Second consequence: with S2 universal, `$ck` at `Analyze-SwydoReport.ps1:424` is never null in the run path, so the `if($null -ne $ck)` omit branch and S5's matching matrix fallback ("where cellKey is absent and the display name is collided, emit reason='no-usable-cell'") are unreachable outside -DefineOnly. AC5 then cannot be exercised end to end. AC11 pins the stamped sequence against the CURRENT extractor only; an archived schemaVersion-2 document produced by any earlier key space gets a manufactured key that its row map does not hold, and the cell silently disappears rather than being omitted with a reason.

**Proposed fix.** Add `Test-Analyze.ps1:1291` and `:1292` to the §4 Migration in-place-update list with the new expectation (both ids resolve, because S2 supplies the key the fixture's row map already carries). Add a Migration paragraph stating that after S2 the no-cellKey omit branch at `Analyze-SwydoReport.ps1:424` and S5's cellKey-absent fallback are -DefineOnly-only, so AC5 must be phrased against a fixture whose row map does NOT contain the derived key (an archived-key-space case), not against a fixture that merely lacks cellKey.

**Skeptic.** Reproduced. Test-Analyze.ps1:1284 builds `$dupNoKey` = ('Clicks', google-adwords:clicks), ('Clicks', facebook-ads:clicks) with no cellKey and runs it through RunAnalyze, whose MkDoc writes `schemaVersion=2` (Test-Analyze.ps1:405) and invokes the real analyzer (:415), so S2 stamps it. Get-UniqKeySeq over that metric list derives 'Clicks' and 'Clicks [facebook-ads:clicks]' — exactly the two keys DupRow's row map already carries (Test-Analyze.ps1:1264). Measured through Get-Breakdown with those keys stamped: valuesById holds BOTH ids, so :1291 (`.Count -eq 0`) evaluates False and :1292 (both id lookups $null) evaluates False. Both are currently-green assertions encoding safety properties — 'the metric is OMITTED from valuesById, never guessed' and 'no wrong number is published' — and §4 Migration names exactly five in-place test updates (:459, :460, :728, :1300, :1301) while §4 Files touched budgets Test-Analyze.ps1 as '§6 cases; five in-place version-pin updates; the stale header comment at :649'. Neither line is listed. Inverting a shipped safety pin unbudgeted is precisely what a Tier-2 migration inventory exists to prevent. The second consequence also holds: S2 is unconditional on a schemaVersion-2 document and Uniq-Key never returns empty (:404 falls back to id then col<idx>), so `$ck` at :424 is never null in the run path, making the :427 omit branch and S5's 'where cellKey is absent and the display name is collided' fallback unreachable outside -DefineOnly — an internal S2/S5 contradiction the spec does not resolve. One sub-claim is overstated and does not carry the verdict: AC5 as worded ('when a matrix contribution cannot resolve its own cell') IS still exercisable end to end, because the existing reason ladder at :830 returns 'no-usable-cell' for an eligible widget with a total row that yields no cell — it just needs a fixture whose row map lacks the derived key, which is the finding's own proposed fix.

### C14 - S8 falsifies the `valuesByIdScope` self-describing marker, which the spec never moves and which two shipped assertions pin

**Severity:** major - **lens:** breakdown - **spec section:** S8 (§2), §4 Migration, §4 Files touched, AC10 (§6)

**Evidence.** `skill/scripts/Analyze-SwydoReport.ps1:446` emits `valuesByIdScope=$(if(@($mets).Count -gt 0){'collisions-only'}else{'none'})` - a hardcoded token. After S8 the map also holds blank-named metrics, so the token states a rule the code no longer follows. The marker exists precisely so the map is self-describing: `Test-Analyze.ps1:1251` pins `-eq 'collisions-only'` with the comment "valuesByIdScope states the rule, so an empty map is not ambiguous", and `Test-Analyze.ps1:1387` pins it again on a collided block. Measured today on a widget with one unique and one blank-named metric: values keys `[Cost]`, valuesById keys `[]`, metricNames `[Cost | ]`, valuesByIdScope `collisions-only`. After S8 the map gains an entry while the token is unchanged. Neither §4 Migration nor the §4 Files-touched table mentions the token or these two pins, and AC10 asserts only presence in `valuesById` and a non-blank `metricNames[]` entry.

**Proposed fix.** S8 gains: "the `valuesByIdScope` token moves from `collisions-only` to `collisions-and-blank-names`; `none` is unchanged." List `Test-Analyze.ps1:1251` and `:1387` in §4 Migration as in-place updates alongside the five version pins. Extend AC10 to assert the token beside the entry, so the map's scope and its self-description cannot drift again.

**Skeptic.** Measured on this tree: a widget carrying ('Cost', google-adwords:cost_micros) and ('', google-adwords:clicks) emits values keys ['Cost'], valuesById keys [], metricNames ['Cost' | ''], and valuesByIdScope 'collisions-only'. The token at Analyze-SwydoReport.ps1:446 is `$(if(@($mets).Count -gt 0){'collisions-only'}else{'none'})` — a hardcoded rule descriptor, accurate today because the :422 gate is exactly `nameCount > 1`. S8 widens that gate to collided-or-blank, so the same widget's map gains an entry while the token keeps asserting a rule the code no longer follows. It is a self-describing marker by explicit design: Test-Analyze.ps1:1251 pins it with the comment 'valuesByIdScope states the rule, so an empty map is not ambiguous', and :1387 pins it again on a collided block. The spec never mentions the token — not in S8, not in §4 Migration, not in the §4 Files-touched row for Test-Analyze.ps1, and AC10 asserts only presence in valuesById plus a non-blank metricNames[] entry. That leaves the implementer with an unspecified fork: leave the token and ship a marker that lies about the map's scope, or move it and red two pins the migration inventory does not budget. Either way the spec must gain the sentence and the two line entries before the build starts.

### C15 - AC18 is unsatisfiable: S10's version markers are unconditional document-wide literals, so no document can flip a breakdown cell with them "untouched"

**Severity:** major - **lens:** breakdown - **spec section:** AC18 (§6), §4 Migration (the OD-3 residual paragraph)

**Evidence.** `skill/scripts/Analyze-SwydoReport.ps1:1276` writes `tool='Analyze-SwydoReport.ps1'; factsVersion=2; canonicalVersion=3; matrixVersion=1` as plain literals with no conditional. S10 rewrites `canonicalVersion` to 4 and `matrixVersion` to 2 for EVERY document, whatever its widgets are. So the fixture AC18 describes (a dimensioned no-total widget with a collided display name) emits `canonicalVersion=4` and `matrixVersion=2` after the change, having emitted 3 and 1 before it. AC18's literal requirement - "`canonicalVersion`, `matrixVersion` and `factsVersion` all stay put" - cannot be satisfied, and §4 Migration's supporting sentence "that flip is reachable with both moved markers untouched" is false. The rest of the AC18 premise does check out: for such a widget `Total-Row` returns $null, so `Get-MatrixContributions` never reaches the read at `:812`, the cell stays rank 0 and its reason ladder result is unchanged, and no headline entry is produced. Only the marker claim is wrong - which is the claim the owner ratified OD-3 on.

**Proposed fix.** Restate AC18 as: "When a dimensioned no-total widget with a collided display name is analyzed, its breakdown cell changes owner while that fixture's `platforms[].headline` and `platforms[].metrics` blocks are byte-identical to the pre-change run, and no emitted marker names the breakdown layer." Replace the Migration sentence with the accurate form: the two markers move on every document because they are unconditional literals, so they signal "new analyzer" but not "the breakdown changed"; the residual is that no marker NAMES the breakdown layer, not that the flip ships with markers untouched.

**Skeptic.** Verified. `canonicalVersion` and `matrixVersion` appear exactly once in skill/scripts/, at Analyze-SwydoReport.ps1:1276, as plain literals in the meta block with no condition of any kind: `tool='Analyze-SwydoReport.ps1'; factsVersion=2; canonicalVersion=3; matrixVersion=1`. S10 rewrites them to 4 and 2, and §4 Migration states the bump is 'global rather than flip-conditional'. So after the change EVERY analyzed document emits 4 and 2, including AC18's dimensioned no-total collided-name fixture. AC18's literal requirement — 'canonicalVersion, matrixVersion and factsVersion all stay put' — cannot be satisfied by any fixture, and §4 Migration's supporting sentence 'that flip is reachable with both moved markers untouched' is false for the same reason. This is not cosmetic: §4 says the OD-3 residual 'is pinned by AC18 rather than left latent', so as written the owner's ratified residual ships with a pin that cannot be made green. The rest of AC18's premise does check out and should survive the rewrite: for a dimensioned no-total widget Total-Row returns $null, so :960 (`if(-not $tr){ continue }`) yields no headline entry and :801/:811 leave the contribution at rank 0 with a null cell, so the :812 read never fires and the reason comes unchanged from the ladder at :832. Only the marker clause is wrong, and it is the clause OD-3 was ratified on.

### C16 - S7's "leaves every key count identical" understates it: on a comparer-equal collision first-wins changes the published key's spelling, and the membership test must be `.Contains`, not `.ContainsKey`

**Severity:** minor - **lens:** breakdown - **spec section:** S7 (§2), §4 Data model, §5 i18n bullet, AC9 (§6)

**Evidence.** `$vals` is `[ordered]@{}` (`skill/scripts/Analyze-SwydoReport.ps1:400`), an OrderedDictionary with a case-insensitive comparer. Measured under PS 5.1: `$o=[ordered]@{}; $o['Clicks']=1; $o['clicks']=2` leaves the stored key as `clicks` - the indexer REPLACES the key casing, it does not preserve the original. So today's last-wins publishes metric N's spelling; first-wins publishes metric 1's: last-wins (today) key: 'Clicks' -> 'clicks' first-wins (S7) key: 'Clicks' Key COUNT is identical, as S7 says, but the published key STRING flips. §4's Data model names "a comparer-equal duplicate display name" as one of the three divergence classes, and §5's i18n bullet lists only "a blank metric label becomes a resolved key", so this user-visible string change is unlisted. It also lands in `metricNames[]` vs the `values` key for a non-PowerShell consumer doing a case-sensitive lookup. Also measured: `[ordered]@{}` has NO `ContainsKey` method (returns False), while the line thirty above the write site uses `$nameCount.ContainsKey($nk)` at `:396` on a plain Hashtable. Under `$ErrorActionPreference='Stop'` a `.ContainsKey` first-wins guard aborts the analyzer at runtime. The correct idiom is already in the file at `:972` (`$platforms[$prov].headline.Contains($key)`).

**Proposed fix.** Restate S7's guarantee as: "the surviving key is the FIRST holder's spelling and the key count is unchanged; on a comparer-equal pair the published key text therefore changes from metric N's casing to metric 1's." Add that string change to §5's i18n bullet and to AC9. Add one sentence to S7 naming `$vals.Contains(...)` as the membership test, because `[ordered]@{}` is an OrderedDictionary and `.ContainsKey` throws in PS 5.1 - the neighbouring `$nameCount` is a Hashtable and the idioms are not interchangeable.

**Skeptic.** Measured under PS 5.1 (5.1.22621.4249) on this machine: `$o=[ordered]@{}; $o['Clicks']=1; $o['clicks']=2` leaves keys [clicks], count 1 - the OrderedDictionary indexer REPLACES the stored key's casing. A plain Hashtable does not (keys stay [Clicks]). So the published `values` key today is metric N's spelling; under S7 first-wins the write is skipped and the key stays metric 1's spelling. Reachable end to end: the extractor's row map at Get-SwydoReport.ps1:728-729 is an [ordered]@{}, so 'clicks' collides with 'Clicks' via Uniq-Key:405 and becomes 'clicks [id]'; Get-Breakdown:404 then reads by name case-insensitively and :409 writes $vals[[string]$m.name], flipping the key. This is not merely an understatement. §4's Migration justifies holding meta.factsVersion at 2 with 'everything here is additive' and 'renaming published `values` keys would have been a shape change' - and S7 renames a published `values` key. Naming the comparer-equal duplicate as a divergence class in §4's Data model does not discharge that: §5's i18n bullet enumerates only 'a blank metric label becomes a resolved key', and AC9 pins display/unit/type but not the key text. AC3 exists but pins collision TREATMENT, not the emitted key string. The build changes: either §5 and AC9 gain the key-text flip, or the factsVersion-stays-2 argument has to be re-made. The .ContainsKey half is confirmed as a type fact with one correction to the finding: the call THROWS ('[System.Collections.Specialized.OrderedDictionary] does not contain a method named ContainsKey'), it does not return False, and Analyze-SwydoReport.ps1:28 sets $ErrorActionPreference='Stop'. Note that the spec never prescribes ContainsKey, so that sentence is an implementation guard rather than a spec defect - it rides along on the confirmed half.

### C17 - S2 silently reverses the shipped valuesById omit rule and reds two end-to-end assertions

**Severity:** blocker - **lens:** disclosure - **spec section:** S2 (and §3 non-goals, §4 Migration/Files touched)

**Evidence.** Test-Analyze.ps1:1283-1292 is an END-TO-END case, not a direct Get-Breakdown call: RunAnalyze (:410-416) invokes the analyzer script and MkDoc (:405) hardcodes schemaVersion=2. Its fixture $dupNoKey is two metrics both displayed `Clicks`, neither carrying cellKey, over a row map DupRow (:1261-1267) keyed `Clicks` and `Clicks [facebook-ads:clicks]`. S2's Resolve-CellKeys calls the shared sequence helper over $w.metrics; Get-UniqKeySeq (Get-SwydoReport.ps1:363-374) returns exactly `Clicks`, `Clicks [facebook-ads:clicks]` for that list -- byte-equal to the row-map keys, because Uniq-Key (Get-SwydoReport.ps1:403) predates schemaVersion 3 and already built those row maps at :727-729. Get-Breakdown's gate at Analyze-SwydoReport.ps1:424 (`if($m.PSObject.Properties['cellKey'] -and $m.cellKey)`) is then satisfied and :433 writes BOTH ids. The two shipped assertions assert the opposite and go red: Test-Analyze.ps1:1291 "P4 AC4 v2 + collided name => the metric is OMITTED from valuesById, never guessed" and :1292 "neither colliding id is addressable, so no wrong number is published". The spec's §4 Migration names exactly five in-place Test-Analyze edits (the version pins at :459, :460, :728, :1300, :1301) and none of them is this; no AC covers it, and the ratified ANLZ-aUniformLattice-8 rule it overturns is still stated in code at Analyze-SwydoReport.ps1:425-426.

**Proposed fix.** Add an S2 sub-clause and an §4 Inventory row: "Resolve-CellKeys makes a collided display name addressable on a schemaVersion-2 document, so valuesById gains entries such a document previously omitted. This SUPERSEDES ANLZ-aUniformLattice-8's 'no cellKey => omit' rule; the comment at Analyze-SwydoReport.ps1:425-426 is rewritten and Test-Analyze.ps1:1291-1292 flip in place to assert PRESENCE with each id carrying its own number." List those two assertions in §4 Migration beside the five version pins, and add an AC: a schemaVersion-2 collided-name e2e fixture yields two valuesById entries whose displays differ.

**Skeptic.** Traced the whole chain and it holds. Test-Analyze.ps1:1284-1292 is end-to-end: RunAnalyze at :410-416 writes the doc to disk and invokes `& $AnalyzeScript`, and MkDoc:405 hardcodes meta.schemaVersion=2. The fixture $dupNoKey at :1284 is two metrics both named 'Clicks' with NO cellKey; DupRow at :1262-1267 builds the row map with keys 'Clicks' and 'Clicks [facebook-ads:clicks]'. S2's Resolve-CellKeys calls the shared sequence helper over $w.metrics. Get-UniqKeySeq (Get-SwydoReport.ps1:363-374) over that list returns exactly 'Clicks' then 'Clicks [facebook-ads:clicks]' (Uniq-Key:403-408: base='Clicks', map empty -> 'Clicks'; base='Clicks', map.Contains -> "$base [$id]"). Byte-equal to the fixture's row-map keys - and byte-equal to real v2 documents too, since the row loop at :728-729 is the same Uniq-Key and the ANLZ-aUniformLattice-2 comment at :709 says the row loop was UNTOUCHED. Get-Breakdown is called at Analyze-SwydoReport.ps1:1253 with the same $dataWidgets objects, so the stamped cellKey reaches it. The gate at :424 (`if($m.PSObject.Properties['cellKey'] -and $m.cellKey)`) is then satisfied for both metrics, :428 resolves each own cell, and :433 writes BOTH ids. So :1291 ('the metric is OMITTED from valuesById, never guessed', asserts count -eq 0) and :1292 ('neither colliding id is addressable') both go red. The spec never accounts for this. §4 Migration enumerates exactly five in-place Test-Analyze edits (:459, :460, :728, :1300, :1301), all version pins. 'Files touched' lists '§6 cases; five in-place version-pin updates; the stale header comment at :649'. §3 asserts the valuesById channel is 'landed fact, not work'. No AC covers it. And the ratified rule being overturned is still written in product code at Analyze-SwydoReport.ps1:425-426. The build changes: an unplanned red gate mid-unit plus an undisclosed reversal of a ratified decision, so the S2 sub-clause, the §4 inventory row and the extra AC in the proposed fix are all warranted.

### C18 - AC14 and AC19's "no candidate equals a superseded value" cannot be asserted on the fixtures they name

**Severity:** major - **lens:** disclosure - **spec section:** AC14, AC19, §7 ("the AC14 closer-arithmetic check")

**Evidence.** Build-FactIndex (Test-ReportNumbers.ps1:195-243) builds ONE flat per-platform ArrayList of [ordered]@{value;type;hasComparison;ulp} with no metric identity attached; the headline contributes only displayCurrent/displayPrevious/displayDelta (:213-218). On the spec's own reproduction (§4 Inventory: `google-adwords:conversions`=100 and `google-adwords:all_conversions`=250, both displayed Conversions) the google-adwords bag is [100,100] today and [100,250] after the repair. The superseded value -- 100, what all_conversions used to publish -- is still a candidate after the repair, because it is `conversions`'s own correct value and always will be. So the assertion is false by construction on the collision fixture, and an implementer will either hit a false failure or quietly write a test that does not check what the AC says. The count clause of AC14 is fine; only the first clause is broken.

**Proposed fix.** Replace AC14's first clause and AC19's second clause with the assertable form: "Build-FactIndex over the collision fixtures yields a candidate multiset identical to the one derivable from headline displayCurrent/displayPrevious/displayDelta, breakdown cells and timeSeries alone -- i.e. canonical.keyBasis and canonical.ambiguousWith contribute ZERO entries to global/byPlatform, and the S14 roll-up contributes nothing to byFid beyond its own count." Add the one-line justification: Build-FactIndex never reads `canonical` and never indexes platforms[].metrics, and findings route to byFid only (:244-254), so the disclosure surfaces are candidate-free by construction rather than by test.

**Skeptic.** Verified against source. Build-FactIndex (Test-ReportNumbers.ps1:195-266) builds one flat per-platform ArrayList of {value;type;hasComparison;ulp} with no metric identity: the headline contributes only displayCurrent/displayPrevious/displayDelta (:215-217), breakdown cells at :226-228, timeSeries at :233-241, and findings route to byFid only (:244-255). The AC's first clause is unsatisfiable BY CONSTRUCTION on any same-type collision fixture, and the spec's own reproduction (§4 Inventory: google-adwords:conversions=100, google-adwords:all_conversions=250, both displayed Conversions) is exactly that. Proof from the analyzer: at :967 both metrics read the SAME cell object ($tr.metrics.$($m.name) resolves to the first holder for both), and the scalar guards at :968-969 accept or reject that one cell for both. So the first holder is published iff the second is - the superseded value (100) is always the first holder's own correct value and is always still a candidate after the repair. An implementer writing 'assert 100 is not in the google-adwords bag' gets a false failure; the only test that passes is one that does not check what the AC says. The clause happens to hold on a cross-unit fixture (AC9's micros-vs-count pair, where the superseded DISPLAY string '5,000,000' vanishes and '$5.00' replaces it), which is worse: the AC looks assertable until someone runs it on the primary fixture. Not taste, not a restatement - the spec has no other sentence covering it, and it changes what gets built (a candidate-multiset equality against the disclosure surfaces, per the proposed edit, versus a value-absence assertion). The count clause of AC14 is fine and the finding says so.

### C19 - S9 and S14 never scope "sharing that display name", and the per-provider reading emits a false disclosure

**Severity:** major - **lens:** disclosure - **spec section:** S9, S14

**Evidence.** Display-name collision is a per-WIDGET property: Get-UniqKeySeq (Get-SwydoReport.ps1:363-374) allocates a fresh $probe per widget metric list, and the row maps are built the same way per widget (:727-729). The headline is per-PROVIDER and merges cells from many widgets (Analyze-SwydoReport.ps1:942-998, dedup at :970-981). Two separate KPI cards -- W1 carrying only `google-adwords:conversions` displayed "Conversions", W2 carrying only `google-adwords:all_conversions` displayed "Conversions" -- have no collision at all: each widget's Uniq-Key sequence yields the bare `Conversions` and each headline cell reads its own widget's row correctly. S9 says only "the other metric ids sharing that display name" and S14 says "per-provider roll-up naming the metric ids whose display names were ambiguous", so a literal per-provider implementation stamps ambiguousWith on both cells and emits a roll-up telling the reader the repair fired where nothing was ever wrong. That layout (two KPI cards, one conversion metric each) is ordinary in Swydo reports.

**Proposed fix.** S9: "...lists the other metric ids sharing that display name WITHIN canonical.sourceWidgetId's own metric list." S14: state that the roll-up is a per-provider PRESENTATION of a per-widget computation, and that an id appears only when some single widget's metric list holds two comparer-equal display names covering it. Add an AC: two single-metric widgets of one provider whose display names are identical produce no ambiguousWith, no roll-up, and keyBasis='display-name'.

**Skeptic.** The per-widget premise is verified at source: Uniq-Key (Get-SwydoReport.ps1:403-409) tests collision against a caller-supplied map, Get-UniqKeySeq allocates a fresh $probe per metric list (:364), and the row maps are rebuilt per widget per row with fresh [ordered]@{} accumulators (:726-729). Get-Breakdown's own collision counter is likewise per widget ($nameCount over $mets at :395-396). The headline is per-PROVIDER and merges across widgets (Analyze-SwydoReport.ps1:942-998), keyed by $m.id at :970, and each cell reads its OWN widget's total row at :967 - so two single-metric KPI widgets of one provider both displaying 'Conversions' have no defect at all: no shared row map, no collision, correct values on both sides. The matrix behaves the same, since groups are keyed (providerId|metricId) at :1052 and each contribution reads its own widget's total row at :812. S9 says only 'the other metric ids sharing that display name' and S14 says 'per-provider roll-up naming the metric ids whose display names were ambiguous'; neither names a scope, and AC19's trigger is written report-wide ('When a report carries an ambiguous display name'), so a per-provider implementation passes every AC while stamping ambiguousWith on two correct cells and emitting a client-facing finding that the repair fired where nothing was wrong. That is precisely the false-signal class the codebase already ratified against in the conflict predicate at :882-884. Reachable on an ordinary two-KPI-card layout, and the fix is a scope clause plus one AC - build-changing, not taste.

### C20 - S15's meta key is unconditional and breaks the shipped "-PeriodKpiFacts omitted => byte-identical" invariant

**Severity:** major - **lens:** disclosure - **spec section:** S15, AC20, §4 Migration

**Evidence.** Analyze-SwydoTrend.ps1:206-207 states the shipped opt-in guarantee: "When -PeriodKpiFacts is OMITTED, emit ZERO RECON_* findings and the trend path stays byte-identical (opt-in; absence is not a data gap)", pinned by Test-TrendAnalyze.ps1:251. The parameter defaults to "" (:25) and $PK is parsed only inside `if($myPeriodKpiFacts){` at :208-211. S15 says flatly that "the trend facts document echoes the source canonicalVersion as one additive meta key" and AC20 covers only the handed-a-document case, so the meta writer at :337-349 gains sourceCanonicalVersion=$null on every ledger-only run. That null is also indistinguishable from the exact case S15 exists to detect: a pre-canonicalVersion archive, where $PK.meta.canonicalVersion is likewise absent and echoes as null.

**Proposed fix.** S15: "the key is emitted ONLY when -PeriodKpiFacts was supplied; with the parameter omitted the key is ABSENT, preserving the opt-in byte-identity documented at Analyze-SwydoTrend.ps1:206-207. Key present with a null value therefore means the source document predates meta.canonicalVersion, which is itself the pre-repair signal." Add AC21: a trend run with no -PeriodKpiFacts produces facts byte-identical to the pre-change output and carries no meta.sourceCanonicalVersion key.

**Skeptic.** Verified, with one correction to the evidence that makes the finding stronger rather than weaker. The opt-in guarantee is real and in-source: Analyze-SwydoTrend.ps1:206-207 states 'When -PeriodKpiFacts is OMITTED, emit ZERO RECON_* findings and the trend path stays byte-identical (opt-in; absence is not a data gap)'. The parameter defaults to "" (:25), $PK is bound only inside if($myPeriodKpiFacts){ at :208-211, and the meta writer at :337-349 sits OUTSIDE that block. S15's wording is flat and unconditional ('The trend facts document echoes the source canonicalVersion as one additive meta key'), and AC20 only covers the handed-a-document case, so the literal build adds sourceCanonicalVersion=$null to meta on every ledger-only run - not byte-identical, and a silent violation of a ratified guarantee. CORRECTION to the finding: Test-TrendAnalyze.ps1:249-251 does NOT pin byte-identity; it collects findings whose ruleId is like 'RECON_*' and asserts the count is zero. The test name says 'byte-identical trend path' but the assertion only counts findings, so nothing in the suite would catch the added key. That removes the finding's claimed gate but confirms the underlying defect and makes an explicit AC21 the only thing that would catch it. The second half also holds: present-and-null would then mean both 'no source document' and 'source predates meta.canonicalVersion', collapsing the exact distinction S15 exists to draw.

